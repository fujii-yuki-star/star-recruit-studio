# α-4 動作確認チェックリスト（開発・社内QA用）

α-3 → α-4 で**追加・改善した機能**を一通り確認するための項目・手順・期待結果。各項目に対応 issue を併記。
チェックは `- [ ]` を `- [x]` に。気づきは各項目の下にメモ。

> 📌 α-3 までの機能（FREE 詳細編集・テンプレ作成・グループ化・掛け合い声調整 等）は `α-3_動作確認チェックリスト.md` を参照。本書は **α-4 の差分**に絞っています。

---

## 0. 前提・準備

- [ ] **環境**：Windows 10/11。**packaged（MSI）での確認を推奨**（声＝VOICEVOX、書き出し＝FFmpeg を同梱で確実に動かせるため）。`npm run tauri dev` でも UI は確認可。
- [ ] **声（VOICEVOX）**：起動直後は読み上げ準備で待つことあり。声が出ない時は少し待つ。
- [ ] **AI**：設定で接続キー or 「お試し」（Mock）。お試しでも一連の流れは確認できる。
- [ ] **後方互換用**：**α-3 で作った既存プロジェクトを1つ**用意（A-1 で使う）。

---

## A. 後方互換（最優先・schema 1.14 → 1.25）

- [ ] **A-1** α-3 で作った既存プロジェクトを開く → **壊れず開ける**。場面・素材・フォント・声・BGM・掛け合い・グループが保持。保存し直し→再読込しても化けない。
  - 観点：1.15（タイムラインの時間軸オーバーレイ）/1.16・1.17（場面ごとBGM・登場アニメ〈キーフレーム〉）/1.18（動画の再生開始タイミング）/1.19（動画クリップの範囲・速度の場面ごと調整）/1.20（自由配置の字幕要素）/1.21（同時開始）/1.22（要素の表示名）/1.23（字幕・文字の背景帯）/1.24（文字の体裁の場面別上書き）/1.25（キーフレームの動き方に自由なカーブ）の追加は**すべて任意フィールド**＝読み込み時に現行へ移行され、未設定は既定を継承。**アニメ・タイムライン調整を一切していない場面は、従来どおり静止1枚**で描画・書き出しされる。
- [ ] **A-2（0.4.3）** **0.4.2 で作ったプロジェクトを開く** → そのまま開ける。**0.4.3 は schema を 1.25 へ上げた**（キーフレームの動き方に自由なカーブを足した＝#262）。**値域が広がっただけの任意項目**なので、移行は版の付け替えのみ＝既存データはそのまま開ける。
  - 📌 `scene.durationSec` の制約を「0 より大きい」に是正したが（#586）、**アプリが作る値は元からこの範囲**で、既存データも読み込みを拒否しない（範囲違反は警告扱い）。

---

## B. タイムライン編集（新画面・ADR-0018・#253 / #322–#331 / #339）★今回の主役

> 開き方：仕上がり**プレビュー画面**の「**タイムラインで見る**」からタイムライン編集画面へ。

- [ ] **B-1 時間軸の射影（#322）**：場面ベースの動画が**1本の時間軸＋トラック**に並ぶ。各場面の尺・順序が時間軸上で正しく見える。
- [ ] **B-2 テロップ／クリップの表示（#323）**：テロップや素材クリップが時間軸上に**帯**で並ぶ。クリックで選択→編集パネル。
- [ ] **B-3 ドラッグ移動・トリム・スナップ（#325–#328）**：帯を**ドラッグして開始位置を移動**、**端をつまんで長さをトリム**。近くの境界に**吸着（スナップ）**する。最小長より短くならない。
- [ ] **B-4 並行テロップ（#339）**：表示時間が**重なるテロップ**は、**段違い（レーン違い）**に並んで重ならず見える。
- [ ] **B-5 タイムラインのテロップ描画（#331）**：タイムラインで置いたテロップが**プレビュー／書き出しMP4に実描画**される（再生位置で表示が切り替わる）。
- [ ] **B-6 取り消し・やり直し（#255）**：タイムラインの編集（移動・トリム等）を **Undo/Redo**（ボタン＋ Ctrl+Z / Ctrl+Y）で戻す・やり直せる。場面編集と同じ操作感。

> ℹ️ 再生ヘッド＋同期プレビュー（再生しながら編集）は **α-5**（ADR-0023）予定。本α版のタイムラインは**構成の見わたし＋テロップ／クリップ調整**が中心。

---

## C. 登場アニメ（キーフレーム・ADR-0019・#254 / #341–#345 / #358）★新機能

> 場所：**場面編集**（FREE 自由配置）。要素またはグループを選んで「登場アニメ」を選ぶ。

- [ ] **C-1 補間の土台＋プレビュー再生（#341 / #342）**：FREE 要素にアニメを付けると、**プレビューの再生位置に応じて補間表示**される（開始→終了へなめらかに）。
- [ ] **C-2 簡易プリセット「ふわっと」（#344）**：場面編集で FREE 要素に**フェードイン**を1タップで付けられる。opacity が text/image にも効く。
- [ ] **C-3 動きプリセット拡張（#345）**：**すべって／ぽん／くるっと**を追加。**速さ・イージング（linear / ease-in-out）／向き**を選べる。
- [ ] **C-4 グループに登場アニメ（#358）**：**グループ全体**にプリセットを付けると、**中の要素がまとめて**動く（合成前 transform 重ね＋メンバー opacity 乗算）。ロック中グループはアニメ操作も無効。
- [ ] **C-5 書き出し一致（パリティ・#343）**：アニメのある場面は**毎フレーム描画→画像列で書き出し**。**プレビューと書き出しMP4で動きが一致**する（ADR-0001／フレーム単位で構造保証）。
- [ ] **C-6 後方互換の維持**：**アニメを付けていない場面は静止1枚のまま**（従来どおり）。掛け合い（複数話者）や動画スロットのある場面はアニメ非適用（プレビュー＝書き出しで同じ扱い）。

---

## D. 場面ごとのBGM（ADR-0018 ③(7)・#336 / #338）

- [ ] **D-1 場面ごとにBGMを選択（#336）**：場面ごとに**別のBGM**を設定でき、プレビューで切り替わって聞こえる。
- [ ] **D-2 書き出しでクロスフェード（#338）**：書き出しMP4で、**曲が変わる境界がなめらかにクロスフェード**する（ブツ切りにならない）。一部BGMの読み込み失敗時は**次の行動を示す文言**が出て、他は継続。音量は範囲内にクランプ。

---

## E. 保存まわりの安心（#256 / #255）

- [ ] **E-1 自動保存（#256）**：編集後に手が止まると**バックグラウンドで自動保存**される。**ドラッグ操作の途中では保存が走らない**（確定後に保存）。
- [ ] **E-2 未保存サイン（#256）**：未保存の変更があると**「未保存の変更があります」**のサイン、保存中は「保存中…」、失敗時は**次の行動を示す文言**。保存が終わるとサインが消える。
- [ ] **E-3 書き出し前の保存と競合しない（#256）**：書き出し直後などに自動保存とぶつかっても、**プロジェクトIDが確定した状態で保存**され、画像等が欠けない。

---

## F. こまかい改善

- [ ] **F-1 「詳細編集」トグル撤廃＝常時表示（#278）**：場面編集で**「詳細編集」の切り替えが無くなり**、画面の切り替え（トランジション）・動画クリップの調整・写真の枠への収め方が**常に見える**。技術用語の漏れなし。
- [ ] **F-2 プロジェクト名の編集（#252）**：ヘッダのプロジェクト名を**その場で変更**できる（blur / Enter で確定・空や未変更は元に戻る・日本語入力の変換確定 Enter で誤確定しない・80字上限）。
- [ ] **F-3 About の使用VOICEVOX一覧（#251）**：「このアプリについて」に、**この動画で使ったVOICEVOXキャラの一覧**が出る（**掛け合いの相手役も網羅**）。
- [ ] **F-4 回転要素の複数リサイズ・グリッド吸着（#300）**：**回転した要素をふくむ複数選択**でもまとめて拡縮できる（枠は回転後の見た目で囲む）。グリッドONのリサイズは**回転後の見た目の角**がグリッドに乗る。固定角を大きく超えて振り切っても**最小サイズに張り付く**（反転して膨らまない）。単一要素の回転リサイズは従来どおり。
  - 📌 α-3 チェックリスト B-8 の「既知：複数同時リサイズで回転要素は対象外」は**本項で解消**。
- [ ] **F-5 場面編集での再生確認（#408）**：場面編集で、**「動き」を付けた要素はその場で再生確認**（「動きを再生」＝書き出しと同じ動き）、**2場面め以降で「切り替え効果」があれば「切り替えを見る」でその場で確認**できる（書き出さずに）。停止すると当該場面の静止表示に戻る。**切り替え方（フェード／スライド）・長さ・境目の字幕/クレジットは書き出しMP4と一致**（動画を含む場面は前後場面の**静止フレーム**を合成＝プレビュー内で動画そのものは再生しない点に注意）。
- [ ] **F-6 場面の並び替え（#398）**：たたき台で場面を**ドラッグして並び替え**できる。場面編集でも**←／→で前後に移動**できる。並びが保存・書き出し順に反映される。

---

## G. 横断チェック（規約・体験・パリティ）

- [ ] **G-1 技術用語が出ない**：画面に JSON/overlay/keyframe/track/speaker番号 等の**技術用語が出ない**（§2-3）。
- [ ] **G-2 エラーは次の行動**：失敗時の文言が**「どうすればよいか」**を示す（§2-5）。
- [ ] **G-3 プレビュー＝書き出し（パリティ）**：登場アニメ・タイムラインのテロップ・場面BGM が、**プレビューと書き出しMP4で一致**（ADR-0001）。
- [ ] **G-4 操作の直感性**：タイムライン・登場アニメ・場面BGM が**迷わずできる**か（要望・引っかかりをメモ）。
- [ ] **G-5 壊れた成功を作らない（#434 / #514）**：動画を置けない場面・**見た目（テンプレ）が見つからない場面**があると、**黙って書き出さず**、公開前チェックが**その場面を示して直す導線**を出し、書き出しも**次の行動を示す文言で止まる**（黙って場面が消えたMP4を作らない）。

---

## H. 0.4.3 の総点検（#547 バッチ／#561〜#590 バッチ）

> 📌 0.4.3 は**新機能ではなく「途中でつまずかない・黙って失敗しない」の総点検**。ここが本命の確認対象。

- [ ] **H-1 書き出しの進捗と終了通知（#547 P2-1 / #589）**：書き出し中に**他の画面へ移っても進捗と戻る導線**が出る。
  終わったら（保存できた／失敗した／中止した）**その画面のまま通知**が出て、「書き出しの画面へ」で移動できる。
  一度見たら消え、**次の書き出しまで出続けない**。
- [ ] **H-2 必ず失敗する状態では先に止まる（#547 P2-5 とその後続）**：見た目が見つからない場面・動画を置けない場面があると、
  **公開前チェックの「このまま書き出す」も、書き出し画面の「動画を保存」も止まる**（どちらの入口でも同じ判定）。
  文言はどの場面をどう直すかを示す。
- [ ] **H-3 場面の種類を変えても中身が消えない（#547 P3-14 / P2-9）**：差し込み先の無い種類に変えても写真の割当が**しまわれ**、
  戻すと**元どおり**。自由配置↔ふつうの往復でも同じ。**動画に出なくなる中身**があるときは切り替え前に確認が出る。
- [ ] **H-4 字幕の「見えない」を減らす（#547 P3-9 / #563）**：置いた字幕が出ないときに**理由と直し方**が出る。
  画面外へはみ出しそうなときは**公開前チェックにも項目**が出る（同時に話すとき／1つの帯が長いときで案内が違う）。
- [ ] **H-5 声づくりの進捗と中止（#547 P2-6 / P3-3）**：「全場面の声を作成」が**たたき台・場面編集・公開前チェックで同じ挙動**
  （進捗が出て、途中で中止できる）。読み上げの長さが**実音声**基準になり、字幕・動きのタイミングがずれない。
- [ ] **H-6 AIに送るものが見える（#547 P2-8 / #585）**：送信前確認で**実際に送る文字**が見える。
  素材が多いと**上位40件に絞り、送らない分の件数**が明記される。
- [ ] **H-7 タイムラインのテロップを短くできる（#561）**：テロップの長さを **0.1秒**まで縮められる（欄の刻みも 0.1）。
  端をつまんで最短まで縮めたとき、長さの表示が**ちょうど 0.1**（`0.0999…` や 17桁にならない）。
- [ ] **H-8 空状態がどの画面でも同じ（#590）**：場面が無い状態で公開前チェック／仕上がり確認／書き出し／たたき台を見ると、
  **同じ見出し・同じ次の行動**が出る。動画案の作成に失敗しているときは**どの画面でも理由と2択**（もう一度試す／手動で場面を作る）。
- [ ] **H-9 こまかい操作（#547 P3 各項 / #565 / #587）**：「今の動画」が**直前に開いていた画面**へ戻る／見た目パターンの重ね順に**↑↓**／
  ウィザードで**選び間違えた素材を外せる**／自由配置の縁取りは**太さを入れるだけで出る**（色見本と実際が一致）／
  重ね順の**↑↓がちょうど1段**動く（立ち絵を飛び越えない）。
- [ ] **H-10 切り替えで場面が消えない（#547 P3-4）**：短い場面に長い切り替え効果を指定しても、**場面が丸ごと消えたMP4**にならない。

---

### メモ・不具合
（画面 / 操作 / 結果 を記録。スクショがあると◎）
