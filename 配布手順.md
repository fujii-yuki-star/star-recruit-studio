# すたりお α版 配布手順

> **社内・限定テスト（α）向け**の配布手順。一般公開・再配布は不可（拘束力ある EULA／MPEG-LA 許諾は社内α据え置き＝`docs/yuko_recruit_docs/13_DEPENDENCIES_AND_LICENSING.md §9`）。
>
> ⚠️ **このファイルに実際の API キーを書かないこと**（リポジトリにコミットされるため＝正典 `13§7` コミット禁止）。キーは **Drive 限定共有版の手順書にだけ**記入する。

---

## 1. 配布物
- **`src-tauri/target/release/bundle/msi/stario_0.4.2_x64_en-US.msi`**（約1.9GB・**未署名**）
- VOICEVOX エンジン・FFmpeg・フォント・標準BGM をすべて内包（**この MSI 1ファイルでOK**）。

## 2. ビルド（再作成する場合）
1. **アプリと VOICEVOX エンジンを終了**してから実行（起動中だと `resources/voicevox_engine` がロックされ `os error 32` で失敗）。
2. `npm run tauri build`
3. 出力：上記 MSI（`productName=stario` のため `stario_..._x64_en-US.msi`）。

## 3. 配布者の手順（α テスター共有フロー）
1. MSI をビルド（または既存を使用）。
2. **α テスト用 Google Drive（「特定のユーザー」だけに共有）** に MSI をアップロード。「リンクを知っている全員」にはしない。
3. **Gemini（動画案の自動作成）を使わせる場合**のキー準備：
   1. **テスト専用の API キー**を発行（本番と分離）。
   2. Google Cloud で **API 制限（Generative Language API のみ）＋予算/クォータ上限**を設定（万一の漏洩に備える）。
   3. **Drive 限定共有版の「テスター案内」にだけキーを記入**（このリポジトリ版＝下記は空欄のまま）。
   - ※ Gemini を使わせない場合はキー不要（アプリは「お試し（Mock）」で動く）。
4. 下記「テスター案内」を Drive 共有のドキュメントに置き、**テスターのアカウントだけに共有**。
5. **α 終了時：API キーを失効**（Drive の版履歴にもキーが残るため、失効で無効化して締める）。

---

## 4. テスターへの案内（このまま Drive 共有版に貼る）

> 実際にテスターへ渡す資料は **[テスターガイド.md](テスターガイド.md)**（テスター向けの完全版）。Drive 共有版ではそちらの「接続キー」欄に発行したキーを記入する。以下はリポジトリ内に残す要約。

> ### すたりお（α・社内テスト版）のインストールと使い方
>
> **動作環境**：Windows 10 / 11（64bit）。初回インストールは**ネット接続推奨**（表示エンジン WebView2 が無い場合に自動取得するため）。
>
> **インストール**
> 1. `stario_0.4.2_x64_en-US.msi` をダブルクリック。
> 2. **「Windows によって PC が保護されました」**と出たら → **「詳細情報」→「実行」**（社内テスト版で署名前のため出ます。問題ありません）。
> 3. 画面に沿ってインストール（容量が大きめなので少し時間がかかります）。
> 4. スタートメニューの **「stario」** から起動。初回は読み上げ音声の準備で少し待つことがあります。
> 5. ファイアウォールの確認が出たら、ローカルの音声エンジン用なので**許可**してください。
> - ※ アプリが開かない場合は「Microsoft Edge WebView2 ランタイム（Evergreen）」を入れて再起動してください。
>
> **AI（動画案の自動作成）を使う場合**
> 1. 左メニュー「設定」→「動画案を作るAI」の接続キー欄に、次のキーを貼り付けてください：
>    - 接続キー：`★★★ ここに発行したテスト用キーを記入（Drive 共有版のみ） ★★★`
> 2. 「接続済み」と表示されればOK。未設定でも「お試し」で動かせます。
>
> **使い方の流れ**
> 新しい動画を作る → 内容と写真・動画を入れる → ゆうこが動画案を作る → 仕上がり確認（BGM・フォントもここで選べます）→ 動画を保存（MP4）。
>
> **お願い・注意**
> - 不具合・気づいた点は〔**連絡先：メール/チャット等をここに**〕までお知らせください（アプリ内「お問い合わせ」は準備中です）。
> - 作成した動画には「VOICEVOX:ずんだもん」のクレジットが常に表示されます（音声の利用規約に基づく仕様です）。
> - これは社内テスト版です。**外部への配布・公開はしないでください。**

---

## 5. 注意（配布者向け）
- **未署名**のため SmartScreen 警告は想定内（上記案内に回避手順あり）。広域配布する段階ではコード署名証明書の導入を検討。
- **キー入りの手順書を git にコミットしない**（このリポジトリ版は必ず空欄プレースホルダのまま）。
- 既知のα制限：一部メニュー（ヘルプ/お問い合わせ/お知らせ）は「準備中」。新規プロジェクトの素材は空から。保存先は「動画を保存」時に毎回選択。
