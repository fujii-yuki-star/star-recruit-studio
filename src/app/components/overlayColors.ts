// キャンバスの**上に重ねて描く**編集の補助線の色（#1108 の調査で見つけた写し）。
//
// ⚠️ **テーマのトークンにはしない**（ADR-0039）＝これはキャンバスの上、つまり
// **利用者の動画の絵の上**に描くもの。背景は動画そのもの（明るいことも暗いこともある）で、
// アプリの見た目（明るい／暗い）とは無関係なので、見た目に合わせて変えると
// **暗い動画の上で見えなくなる**。どちらの見た目でも動画の上で目立つ色を固定で持つ。
//
// ⚠️ **写して増やさない**＝同じ値が `FreeLayoutOverlay.tsx` と `TemplateLayerOverlay.tsx` に
// 別々に書いてあった（片方だけ変える事故が起きる形）。

/**
 * 吸着ガイド線の色。
 *
 * 選択枠（`--color-primary` の青緑）と**区別できる**別のアクセント色にしてある
 * ＝「いま何に吸い付いているか」と「何を選んでいるか」を取り違えさせない。
 */
export const SNAP_GUIDE_COLOR = "#ff3d8b";
