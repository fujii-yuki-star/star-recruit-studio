# 15. エラー・状態モデル

> 散在していたエラー表示・状態（音声生成／レンダリング等）を一元化する正典。`schemas/project.schema.json` の `Warning`（`code`/`message`/`field`/`severity`/`autoFixed`）の **`code` 語彙はここで定義**する。
> ユーザー向け文言の原則（原因でなく次の行動）は `01 §15.5`・`05 §17`・`06 §16` を統合。

---

## 1. 状態の一覧（永続 / 実行時）

| 状態 | 値（enum） | 永続/実行時 | 出典 |
|---|---|---|---|
| `narration.status` | none / pending / generated / failed | **永続**（Scene内） | `11 §3.5` |
| `renderStatus` | idle / preparing / rendering / encoding / done / error / unsupported / cancelled | 実行時（書き出しジョブ） | `11 §3.5` |
| `aiGenerationStatus` | idle / sending / generating / validating / done / failed | 実行時（AIジョブ） | 本書 |

> `aiGenerationStatus` と `renderStatus` は**ジョブの実行時状態**で、`project.json` には持たない（MVP）。`narration.status` のみシーンに永続する。

---

## 2. 状態遷移図

### 2.1 音声生成（シーン単位）

```text
none ──(セリフ確定/生成指示)──▶ pending ──(生成成功)──▶ generated
  ▲                               │
  │                               ├──(失敗)──▶ failed ──(再試行)──▶ pending
  │                               ├──(失敗・作り始める前が generated)──▶ generated へ戻す（#755-3）
  │                               │
  ├───────(一括作成を中止)─────────┘
  │
  └──────────(セリフ変更で要再生成)────────── generated
```

- `generated` のシーンのセリフを変更したら `none`（または `pending`）へ戻す＝**再生成が必要**な状態にする（`01 §8.5`）。
- 失敗は該当シーンのみ。他シーンの音声は維持。
- **声の一括作成（全場面）はいつでも中止できる**（#547 P2-6・書き出しの中止＝§2.3 と同じ「エラーではない中立状態」）。意味は
  「**これ以上新しく作らない**」だけで、**できあがった声（`generated`）は取り消さない**。中止後は同じボタンでそのまま続きを作れる。
- 中止した時点の `pending` は、**実行中のぶんも含めてすべて `none` へ戻す**。残すと進捗が止まったまま固まり、`pending` を見て止まる
  書き出しのブロック（§4）も解けず、その行だけ作り直しもできない行き止まりになる（§2-5・ADR-0026④）。`pending` の行にはまだ音声が
  無いので失う物は無い。実行中だった合成は、届けばそのまま `generated` になる（作った音声は無駄にしない）。ただし
  **中止直後に作成をやり直すと、その行は作り直しになる**（後発の合成が正となり、先に走っていたぶんは使われない）。
  **中止直後に書き出しを始めた場合**も、届いた音声は書き込まず `none` に戻す（開始時のスナップショットに無い音声を足すと
  「画面だけ新・動画は無音」になるため＝#570 が優先。書き出し後に作り直せる）。
- `pending` は「そのとき合成が走っていた」という**実行時の状態**であり、保存や履歴に残す意味がない。読込（前回の合成中に閉じた）と
  取り消し/やり直し（作成中に別の編集をして積まれたスナップショット）でも `none` へ戻す＝**誰も作っていない準備中**を作らない。
- 中止を**実際に効かせる**ため、一括作成は同時に走る合成の数を絞る（全件を一度に投げると待機列が空になり、止めるものが無い
  ＝ボタンはあるのに止まらない・ADR-0026④）。進捗は「声 3/10」のように**行単位**で示し、中止後も分数を残す
  （作成済みが消えていないことが数字で分かる）。たたき台・場面編集・公開前チェックの3画面で同じ表示・同じ挙動にする（ADR-0026②）。

### 2.2 AI構成案生成

```text
idle ─(送信確認OK)─▶ sending ─▶ generating ─▶ validating ─▶ done
        │                                          │
        └─(キャンセル)─▶ idle                       └─(重大不適合/パース不能)─▶ failed
sending/generating ─(通信失敗)─▶ failed ─(再試行 / 手動作成 / 前回復元)─▶ …
```

- `validating` で軽微な問題は**自動補正**（`11 §9`）して `done`、重大なら `failed`（`12 §9.3`）。

### 2.3 書き出し（シーン単位 → 結合）

```text
idle ─(開始)─▶ running[ scene 1..N を順次レンダ → 結合 → 音声ミックス ] ─▶ completed
                     │
                     ├─(あるsceneで失敗)─▶ failed（失敗sceneを特定）
                     │      └─(そのsceneだけ再実行 / 中断)
                     └─(ユーザーが中止)─▶ cancelled ─(再実行)─▶ idle
```

- シーン単位レンダ（`05 §6`）。完了済みシーンの一時成果物は保持し、**失敗シーンのみ再実行**できる。
- **終わったことは書き出し画面以外でも知らせる**（#589）。書き出し中は他画面へ移動できる（§4）ため、終端で編集ロックの
  バナーが消えるだけだと**離席中に失敗しても「消えた＝成功して終わった」と誤読**し、書き出し画面へ戻るまで気づけない。
  アプリ共通の通知（成功/失敗/中止・失敗は読み上げにも割り込む）を**どの画面にいても**出し（独自ヘッダの画面も含む）、
  「書き出しの画面へ」で理由・保存先へ導く（§2-5）。**書き出し画面では出さない**（そこに結果がある）＝
  **開いた時点で既読**にし、「閉じる」でも消せる。次の書き出しを始めれば未読は落ちる＝古い通知を残さない（P3-11 と同じ轍を踏まない）。
- **終わった状態（completed/failed/cancelled）は次の書き出しまで残る**（実行時状態・§1）。進捗を全画面に出す（§4）ための保持だが、
  その結果**画面を離れて戻ると終わった結果が出続ける**。表示側で「**前回の書き出し**の結果」と分かる形にし、進捗そのもの
  （%・バー）はいま走っている書き出しにだけ出す（#547 P3-11・ADR-0026④＝そのあとの編集まで書き出し済みに見せない）。
  保存したファイルへの導線は残す（前回の結果でも、そのファイルの話としては正しい）。文言・出し分けは `06_UI_SPEC §13`。
- **書き出しはいつでも中止できる**（#380）：中止は「次の行動」を残す逃げ道（§4 の編集ロック中の唯一の抜け道）。中止すると走行中の変換プロセス（ffmpeg）を終了し、`cancelled`（＝エラーではない中立状態）にする。中止後は待たされず再実行できる（一時成果物は次回書き出し冒頭で掃除）。**書き出し中にアプリを閉じても変換プロセスは終了する**（プロセス残留＝orphan を防ぐ）。

---

## 3. 部分失敗時の挙動

| 場面 | 挙動 |
|---|---|
| 書き出し中に1シーン失敗 | 失敗シーンを特定（`05 §16`）。①そのシーンだけ再実行 ②中断 を選べる。完了分は破棄しない |
| 音声生成失敗 | 該当シーンのみ。**作り始める前が `generated`（＝いまの文の声が既にある）ならその印へ戻す**、それ以外は `failed`（#755-3）→ 再試行。他は維持。⚠️ **知らせは印に紐づけない**（据え置くと出なくなる＝無言の失敗） |
| AI生成失敗 | 前回 `ai/latest_result.json` から復元可（`12 §9.3`） |
| 一部素材が欠損 | 該当シーンに警告。書き出しは「該当箇所を空/代替」で続行可否を提示 |
| 動画スロットを配置できない（内部不整合／スロット・グループの非表示等で分割不可） | **黙って静止画化しない**（#434・ADR-0026）。書き出しは停止し「場面Nの動画を配置できませんでした。場面編集で置き直して再実行」を示す。公開前チェック（precheck）でも該当場面つきで事前警告する |
| 字幕が画面からはみ出す（同時字幕の段積み／1帯が大きすぎ・長すぎ） | **黙って画面外に切らない**（#533 P2・#563・ADR-0026④）。判定は**実描画と同じ `layoutScene`** を通す共有関数 `subtitleOverflowsCanvas`（全字幕層・全辺・回転/グループ transform/非表示込み）＝画面の見え方と警告が食い違わない。**掛け合いに限らず単独/逐次も対象**（#555 で場面ごとに字幕を拡大できるようになり到達性が上がった）。場面編集は**原因ごとに次の行動**を出し分け（同時＝セリフを減らす／1帯＝文字を小さくする・字幕を短くする・§2-5）、公開前チェックでも該当場面つきで事前警告する。**書き出しは止めない**（「直せば良くなる」警告側）。標準テンプレ×既定の体裁では推奨上限の倍の長さでも警告しない（ノイズを出さない＝回帰テストで固定） |
| 置いた字幕が何も表示しない（場面の字幕がオフ／字幕の文が空／対象に選んだ話者のセリフが無い） | **黙って出ないままにしない**（#547 P3-9・ADR-0026④）。自由配置の字幕は表示文を「対象」から解決するため、原因が要素の外（場面のスイッチ・セリフごとの字幕・話者の顔ぶれ）にあり、置いた本人には見えない。場面編集の字幕欄に**理由と次の行動**を出し、公開前チェックでも該当場面つきで事前警告する。**書き出しは止めない**（字幕1つが出なくても動画は作れる＝「直せば良くなる」警告側）。判定は描画と同じ単一の参照元（`subtitleSilentReason`→`freeSubtitleElementTexts`）＝画面の表示と警告が食い違わない |
| 文字が枠に入りきらず切り詰められる（末尾が `…` になる） | **黙って切ったままにしない**（#346・ADR-0026④）。⚠️ **字幕のはみ出し（画面の外へ出る）とは別の壊れ方**＝こちらは**画面の中で完結する**ので、見ただけでは「そう書いたのか」「切れたのか」が分からない。判定は**実描画と同じ数字**（`sceneDrawnLayouts` が返す文字アイテムの幅・字の大きさ・行数の上限）を `wrapText` に通す＝チェックは通ったのに動画では切れている、を作らない。⚠️ **`sceneDrawnLayouts` を通すのが肝**＝`layoutScene` を opts なしで1回呼ぶと、掛け合いの場面では**実際に描かれる行ごとの字幕を一度も見ず**、代わりに**動画に出ない静的字幕**を見てしまう（見落としと誤検出が同時に起きる）。⚠️ **「末尾が `…`」では判定しない**＝利用者が自分で「つづく…」と書いた**枠に収まっている**文字まで拾い、示した次の行動に従っても**永久に消えない**（§2-5＝行き止まり）。**切った事実**（折り返した結果と元の文の違い）で見る。⚠️ **「字幕の長さ」で既に知らせた場面は出さない**＝原因も直し方も同じものを2行にしない（横型の標準テンプレは字幕の枠が 74 字ぶんあり、60 字で既に出ている）。縦型は枠が 44〜46 字ぶんなので**45〜60 字はこちらだけで拾う**（項目自体は本物の穴を塞いでいる）。**書き出しは止めない**（「直せば良くなる」警告側） |
| 素材が引き伸ばしでぼやける（描く枠より元の絵が小さい） | **注意だけ**（#346）＝そのままでも動画は作れる。⚠️ **「小さい」だけでは出さない**＝ロゴのように小さく置く素材は元が小さくてよい（**描かれる枠と比べる**）。⚠️ **収め方で式が違う**＝`cover` は枠を覆うので縦横のきつい側、`contain`（ロゴ・立ち絵の既定）は枠に収めるのでゆるい側が実際の倍率。きつい側で見ると**枠と縦横比が違うだけのロゴを誤検知**する。⚠️ **大きさが測れていない素材は出さない**（§2-5＝直しようが無い注意を出さない）＝そのため**取り込み時に写真も測る**（`probeImageSize`）。測らないと**写真では一度も出ない**注意になる |
| 表示時間に対してセリフが多い（早口になる） | **注意だけ**（#346）。⚠️ **「セリフの長さ」（文字数そのもの）とは別**＝こちらは**尺との釣り合い**で、短い場面に長いセリフを入れると**声が鳴り終わる前に場面が切り替わる**。⚠️ **同時に流す行（ADR-0031）は足さない**＝2人が同じ窓でしゃべるので、素朴に合算すると**人数ぶん二重計上**する。窓の分け方は再生・書き出しと同じ `groupIndices` を通し、グループごとに**いちばん長い行**で見る |
| **書き出しが必ず失敗する項目が残っている**（見た目が見つからない／動画を配置できない／再生開始が「アニメの後」なのに再生されない） | 公開前チェックの主ボタン「このまま書き出す」を**押せなくする**（#547 P2-5）。保存先を選ばせた後に落とさない＝手戻りを作らない（ADR-0026④）。理由（どの項目か）と次の行動（上の「直す」から直す）を併記する。**見た目が見つからない場合は明示操作「まとめて標準にする」**でまとめて標準の見た目へ寄せられる（自動では置換しない＝§6 `TEMPLATE_NOT_FOUND` ②）。**「直せば良くなる」警告（声が未作成・字幕が長い・切り替えに飲み込まれる・切り替えが表示時間に収まらず短くした〔#727〕）では止めない**＝書き出せるのに押せない、を作らない。止める条件は書き出し側の停止条件（`buildExportScenes`）と**同値に保つ**（片方だけ変えると行き止まり／取りこぼしになる）。**同値は「同じ条件を書き写す」のではなく共有の述語を呼んで構造で担保する**（#588：再生開始＝アニメ後は `afterAnimNeverPlaysForSlots` を precheck・書き出しの両入口が呼ぶ。書き出しはスロットをコールバックで受け取るため、解決済みスロットを受ける形にしてある）。**実経路（実 `buildExportScenes`）で「precheck が挙げる ⟺ 書き出しが止まる」を突き合わせる回帰テスト**を置き、条件を書き直したドリフトが赤くなるようにする。**同じ述語（`exportBlockingItems`）を書き出し画面の「動画を保存」にも適用**する＝サイドバーから直行した経路でも同じ判定（ADR-0026②）。押下前に無効化して理由と次の行動（公開前チェックで直す）を出し、開始時は `beginExport` の前後2回の再確認でも同じ述語を使う。**この端末で書き出せない（h264 不可）**場合も同様に両画面で止める |

---

## 4. 排他制御（MVP）

- **書き出し（export）実行中**：プロジェクト編集をロックする（または読み取り専用スナップショットに対して実行）。ロックの唯一の抜け道は**「書き出しを中止」**（#380・§2.3）＝走行中の変換を止めてロックを解く。**書き出し中もどの画面へも移動できる**ため、ロック中は**全画面**で進捗（%＋いま何をしているか）と書き出し画面への復帰導線を出し、抜け道に到達できる状態を保つ（#547 P2-1）。進捗が書き出し画面にしか無いと、離れた利用者には止まったように見え、二重書き出しを誘発する（ADR-0026②）。
- **プレビュー再生中**：編集は可。変更は次回プレビューから反映。
- **音声生成中のシーン**：そのシーンのセリフ編集はキュー化 or 警告。
- **声の一括作成中**：書き出しは開始できない（開始時スナップショットに無音の場面が入るため・#570／ADR-0026④）。ロックの抜け道は
  **「声の作成を中止」**（§2.1）＝待機分を `none` へ戻して止め、そのまま書き出しへ進める。逆に**書き出し中は声を作らない**（同）。
- **タイムライン形式の書き出し開始**（#718）：始められない理由を**1つの述語**（`exportStartBlock`）で見る＝
  **素材の取り込み中／声を作っている最中／別形式の書き出し中／直前の回の後片づけ待ち（`EXPORT_CLEANUP_PENDING`・#843）／
  文書の中身の理由（`TIMELINE_EXPORT_*`）／この端末で書き出せない**。押す前に無効化して理由を出し、押したときの断りも同じ述語から来る（§3 の流儀）。
  とくに**声を作っている最中**は、始めてしまうと合成の着地が `TIMELINE_EDIT_EXPORTING` で撥ねられ、
  **作った声はファイルだけ残って文書に入らない**＝その読み上げが無い動画が成功として出る（ADR-0026④）。
  **直せる理由を先に出す**（この端末では書き出せない、は最後）＝直せば書き出せることに気づけるようにする。
- **タイムライン形式で画面を離れる**（#719）：**入口を問わず同じ関門を通す**（`navigationGuard`＝`App` の遷移が聞く）。
  保存が済んでいなければ**書き切ってから離れ**、失敗していたら**行き先つきで確認**する。**書き出し中は離れない**
  （進み具合も中止もこの画面の中にしかない）＝確認ではなく理由で断る。画面内のボタンだけに確認を付けると、
  サイドバーから素通しできる（実際にそうなっていた）。
- 同一プロジェクトの**同時多重編集は想定外**（MVP）。

---

## 5. エラー分類（taxonomy）

| 軸 | 値 |
|---|---|
| `severity`（`Warning.severity`） | `info` / `warning` / `error` / `fatal`（fatalは処理停止） |
| 回復性 | `auto-fixed`（自動補正済み） / `user-action`（要操作） / `retryable`（再試行可） / `fatal` |

**表示原則**：原因でなく**次の行動**を示す。技術詳細は詳細モード／ログのみ（`01 §15.5`）。自動補正は「3件を自動調整、1件は確認が必要」のように件数＋対応内容で見せる（`01 §6.7`）。

---

## 6. エラーコード体系（`Warning.code` 語彙）

- 命名：`DOMAIN_REASON`（大文字スネーク。例 `TEMPLATE_NOT_FOUND`）。`schemas` 上は自由文字列だが、**実装は本表の語彙を使う**。
- 代表コード（増えうる。追加時は本表へ）：

| code | severity | 既定の自動対応 | ユーザー向け文言（例） | 由来 |
|---|---|---|---|---|
| `TEMPLATE_NOT_FOUND` | warning（②は公開前チェックで「要対応」として出す＝`PrecheckItem` 側の値。`Warning.severity` は §5 の3値） | **状況で分ける**。①**取り込み時**（AI出力→場面への変換）は同category・同orientationへ**自動補正**（`12 §8`・§2-2 の「検証→自動補正→変換」。場面はまだ作り立てで、失われる作り込みが無い）。②**既存の場面**が後から解決できなくなった場合は**自動では置換しない**＝書き出しを停止し公開前チェックで該当場面を示す。標準へ寄せるのは**利用者の明示操作**「まとめて標準にする」（作り込んだ配置が黙って別物に化けるのを防ぐ）。③**マイ見た目の削除と同時**は、開いているプロジェクトの使用場面を標準へ置換する（#458・孤立参照を作らない）。これは削除確認で**影響する場面数を先に示した**うえでの明示操作として成立させる（他プロジェクトの場面は②に落ちる） | ①この場面の見た目パターンが見つからないため、標準を使います ②この場面の見た目パターンが見つかりません。場面編集で選び直すか、「まとめて標準にする」で標準の見た目に変えてください | `11 §9`・`12 §8`・#547 |
| `TEMPLATE_ORIENTATION_MISMATCH` | info（補正時）/ warning（補正不可時） | 同category・同orientation へ置換（**取り込み時はマイ見た目を当て先にしない**＝ADR-0017／利用者の向き変更ではマイ見た目も可） | 動画の向きに合う見た目に調整しました | `ADR-0012` / `11 §9` |
| `ASSET_NOT_FOUND` | warning | null＋候補提示 | 使う写真・動画が見つかりません。選び直してください | `05 §17` |
| `ASSET_FILE_MISSING` | warning（公開前チェックでは**使っている素材のみ**「要対応」＝`PrecheckItem` 側の値。使っていない素材は動画に入らないので出さない） | **選び直し導線**（#347・α-6 で実装）＝`assetId` は変えずに**ファイルだけ差し替える**（ADR-0024＝Asset は元素材の源泉）ので、置いた場所・切り出す範囲・キーフレーム・字幕の紐づけは**構造的に**残る。⚠️ **黙って別の素材にしない**（`§2-5`・ADR-0026④）。差し替えた素材が短く**切り出す範囲が新しい長さを超える**ときは収め直し、**収め直したことを知らせる**（`clipClampedMessage`）。⚠️ **種類の違うファイルへは差し替えない**＝写真↔動画で入れ替えると、種類を変えれば**置いた差し込み口が受け付けなくなって黙って消え**、種類を変えなければ**写真として動画を描く**ことになり何も映らない（どちらも「黙って別の結果」）。断って**代わりの手**（取り込んでから場面で選び直す）を示す。調べるのは**アプリの中だけ**＝ブラウザにはプロジェクトフォルダが無く、「全部見つからない」は嘘になるので**項目そのものを出さない** | ①（素材の画面・一覧）〇つの素材のファイルが見つかりません。動かしたか、消えている可能性があります。その素材を選んで「ファイルを選び直す」から入れ直してください（置いた場所や設定はそのまま残ります） ②（公開前チェック）動画で使っている素材のファイルが見つかりません（〇〇）。素材の画面で「ファイルを選び直す」から入れ直してください | `05 §17`・#347 |
| `REQUIRED_SLOT_EMPTY` | warning | — | この場面に必要な写真・動画が未設定です | `11 §8 V6` |
| `DURATION_CLAMPED` | info | clamp | 表示時間を見やすい長さに調整しました | `11 §9` |
| `TEXT_OVERFLOW` | warning | 短縮提案 | 文字が表示範囲に収まりません。短くできます | `05 §17` |
| `POSE_FALLBACK` | info | 既定yukoへ | ゆうこの表情を標準に調整しました | `12 §8.3` |
| `NARRATION_EMPTY` | warning | 生成前に警告 | セリフが空です。入力してください | `08 §10` |
| `VOICE_ENGINE_UNAVAILABLE` | error | 設定確認導線 | ゆうこの声の準備ができていません。設定を確認してください | `05 §17` |
| `VOICE_GENERATION_FAILED` | error | 該当シーン再試行 | ゆうこの声の作成に失敗しました。もう一度お試しください（**前の声がそのまま使えるときは** `KEPT_PREVIOUS_VOICE_SUFFIX`＝「前に作った声はそのまま使えます。」を添える・#755-3） | 添え書きの条件＝**印を据え置いた**かつ**鳴らす材料がある**（印だけで言うと、読み込めていない声にも「使えます」と言う） |
| `AI_RESPONSE_INVALID` | error | 再生成/手動 | 動画案の作成に失敗しました。もう一度お試しください。手動でも始められます | `12 §9.3` |
| `EXPORT_FAILED` | error | 失敗シーン特定/リトライ | 動画の保存に失敗しました。ログを確認してください | `05 §17` |
| `FFMPEG_NOT_FOUND` | fatal | — | 動画の保存機能の準備が不足しています | `08 §10` |
| `BGM_FILE_BROKEN` | warning | BGMなしで続行を提示 | BGMを読み込めませんでした。BGMなしで続けられます | `08 §10` |
| `TOTAL_DURATION_EXCEEDED` | warning | — | 動画が長すぎます。場面を減らすか短くしてください | `11 §8 V9` |
| `TOO_MANY_SCENES` | warning | — | 場面が多すぎます。整理をおすすめします | `11 §8 V10` |
| `FREE_ELEMENT_OUT_OF_BOUNDS` | warning | — | 画面の外にはみ出した配置があります。画面内に移動できます | `ADR-0008 §8` |
| `FREE_ELEMENT_INVALID_SIZE` | warning | — | 配置した要素の大きさが正しくありません。幅と高さを設定し直してください | `ADR-0008 §8` |
| `FREE_TEXT_EMPTY` | info | — | 文字が入っていない配置があります。文字を入力してください | `ADR-0008 §8` |
| `LINE_ID_DUPLICATE` | warning | 案内のみ（UI採番は一意で通常出ない） | セリフの並びに重複があります。作り直してください | `ADR-0015 §8 V16` |
| `LINE_START_OUT_OF_RANGE` | warning | 案内（入力は[0,場面尺]へclamp・描画も clamp） | セリフの開始位置が場面の長さを超えています。場面の長さ内にしてください | `ADR-0015 §8 V17` |
| `LINE_ORDER_INVALID` | warning | 時間順へ並べ直し | セリフの開始順序が前後しています。時間順に並べ直せます | `ADR-0015 §8 V18` |
| `LINE_SPEAKER_UNKNOWN` | warning | 既定の声へ | 選べない声が指定されています。標準の声を使います | `ADR-0015 §8 V19` |
| `TIMELINE_TRACK_NOT_FOUND` | warning | — | どの列に置くか決まっていない部品があります。列を選び直してください | `ADR-0032`・`11 §8 V22` |
| `TIMELINE_CLIP_TRACK_KIND` | warning | — | 音の部品は音の列に、絵や文字の部品は映像の列に置いてください | `ADR-0032`・`11 §8 V23` |
| `TIMELINE_CLIP_OVERLAP` | warning | — | 同じ列で時間が重なっています。ずらすか、列を増やして重ねてください | `ADR-0032`・`11 §8 V24` |
| `TIMELINE_AUDIO_SOURCE_CONFLICT` | warning | — | 音の出どころが2つ指定されています。どちらか一方にしてください | `ADR-0032`・`11 §8 V25` |
| `TIMELINE_VOICE_TEXT_EMPTY` | info | — | 読み上げる文が入っていません。文を入力してください | `ADR-0032`・`11 §8 V28` |
| `TIMELINE_VOICE_ON_NON_VOICE` | warning | — | 読み上げの中身は読み上げの部品にだけ置けます。読み上げの部品に置き直してください | `ADR-0032`・`11 §8 V28` |
| `TIMELINE_SUBTITLE_LINK_NOT_FOUND` | warning | — | 連動する読み上げが見つかりません。連動先を選び直すか、連動をやめてください | `ADR-0032` 決定24・`11 §8 V29`（字幕は自分の文へ落ちて描かれ続ける＝黙って連動が切れたことに気づけない） |
| `TIMELINE_CROP_HIDES_ALL` | warning | — | 切り抜きで全部隠れている部品があります。切り抜きを小さくしてください | `ADR-0032`・`11 §8 V30`（描画は 1px 残す＝丸ごと消えたことに気づけるようにする） |
| `TIMELINE_ANIMATION_DUPLICATE` | warning | — | ひとつの部品に動きが2つ以上あります。あとの1つは動画に出ないので、動きを付け直してください | `ADR-0032`・`11 §8 V31`（読む側は `targetId` で1本しか見ない＝2本あると片方が黙って無視される・#717） |
| `TIMELINE_DUPLICATE_ID` | warning | — | 同じ部品が二重に入っていて、正しく表示できません。その部品を消して置き直すか、元の動画の「タイムラインで編集する形にする」から作り直してください | `ADR-0032`・`11 §8 V32`（読む側は id で引き当てる＝重なると別のものに効く・#811） |
| `TIMELINE_SUBTITLE_LINK_ON_NON_SUBTITLE` | warning | — | 読み上げと連動できるのは字幕の部品だけです。字幕の部品に置き直してください | `ADR-0032` 決定24・`11 §8 V29` |
| `BAKE_DIALOGUE_SUBTITLE_SKIPPED` | info | — | セリフに合わせて切り替わる字幕は持っていけません。作ったあとに字幕を置き直してください | `ADR-0032`・`11 §7.6.1.1`（**通常の見た目の字幕層は #633 で焼けるようになった**＝残るのは自由配置の字幕ボックスで対象が行に追従するもの〔全部／話者〕だけ） |
| `BAKE_VIDEO_START_TIMING_SKIPPED` | info | — | 動画を再生し始めるタイミングは持っていけません。作ったあとに動画の位置で調整してください | `ADR-0032`・`11 §7.6.1` |
| `TIMELINE_OVERLAY_RETIRED` | info | データは残す（消さない） | この動画には以前この画面で直した時間の流れが残っていますが、動画には反映されなくなりました。内容は消していません。時間の流れを細かく作るときはタイムライン編集用に書き出してください | `ADR-0032` 決定11/12・`11 §1`（#635。**黙って消さない**＝ファイルには残し、描画・書き出しに出さないことだけを伝える） |
| `USER_FONT_MISSING` | error | 書き出しを止める（描画は既定へ倒す） | この動画で使っている文字の形（フォント）が N つ見つかりません。このまま書き出すと別の字になります。設定の「文字の形」から取り込み直すか、使っている場面で別の文字の形を選び直してください | `ADR-0038`・#261。⚠️ **黙って別の字体の動画を成功として出さない**（§2-5）＝画面は既定の字体で描いてよい（真っ白にしない）が、**書き出しは止める**。⚠️ **「調べていない」と「そろっている」を分ける**＝一覧を取れない場では項目そのものを出さない（`missingAsset`／#347 と同じ流儀）。出どころ＝公開前チェックの「見つからない文字の形」（`blocksExport`）|
| `USER_FONT_IMPORT_FAILED` | error | 取り込まない | このファイルは文字の形として読み込めません。ttf・otf・woff・woff2 のいずれかを選んでください | `ADR-0038`・#261（利用者判断＝形式は4つとも）。⚠️ **読めない字体を一覧に載せない**＝載せると「選べるのに描けない」になる |
| `SCHEMA_VERSION_UNSUPPORTED` | fatal | — | このファイルは対応していないバージョンで作成されています | `11 §1` |
| `PROJECT_FORMAT_UNSUPPORTED` | fatal | — | この動画はタイムラインで編集する形式です。場面の編集画面では開けません。一覧から別の動画を選んでください | `ADR-0032`・`11 §1` |
| `TIMELINE_FORMAT_UNSUPPORTED` | fatal | — | この動画はタイムラインで編集する形式ではありません。一覧から別の動画を選んでください | `ADR-0032`・`11 §1`（`PROJECT_FORMAT_UNSUPPORTED` の逆向き） |
| `TIMELINE_VERSION_UNSUPPORTED` | fatal | 同メジャーは現行版へ引き上げ | この動画は対応していないバージョンで作成されています。アプリを更新してください | `ADR-0032`・`11 §1`（**「形式」と言わない**＝形式は場面/タイムラインの別を指す語） |
| `PROJECT_NEWER_VERSION` | fatal | — | この動画は、いまのアプリより新しい版で作られています。アプリを更新してから開き直してください | `#793`・**両形式共通**（`domain/schemaVersionCompare.ts`）。⚠️ **「壊れています」と言わない**＝文書は壊れておらず、**アプリを更新すれば開ける**。⚠️ **「別の動画を選んでください」も書かない**＝**別のを選んでも解決しない**（§2-5＝実行しても直らない行動を名指ししない）。**穴の形**＝両形式の版の関門は**メジャーしか見ない**ので、**同メジャーの新しいマイナー**（場面 1.26／タイムライン 1.9 等）はそこを通り抜け、migrate が版を**現行へ書き下げ**たうえで ajv が落ち、`PROJECT_DOC_BROKEN`／`TIMELINE_DOC_BROKEN` の**嘘の案内**になっていた。**引き上げる前に断る**。⚠️ **マイナーは数で比べる**（文字列だと `1.9 > 1.10` になる） |
| `TIMELINE_DOC_BROKEN` | fatal | — | この動画の内容が正しくありません。一覧から別の動画を選んでください | `ADR-0032`・`11 §8 V1/V2` |
| `TIMELINE_OPEN_FAILED` | fatal | — | この動画を開けませんでした。一覧から選び直してください | `ADR-0032`（読み出しそのものの失敗＝生のエラーを出さない） |
| `TIMELINE_SAVE_FAILED` | error（回復性＝`retryable`。編集内容そのものが残らない＝`EXPORT_FAILED` と同じ段。`warning` 帯の `TIMELINE_EDIT_*` は「置けないだけでデータは無事」なので別） | **自動では繰り返さない**（画面の「保存し直す」で再試行）・**画面を移る前に確認する** | 変更を保存できませんでした。もう一度「保存し直す」を押してください。押しても直らないときは、直前の操作を取り消してからお試しください | `ADR-0032`・`06 §12.1`（#693。タイムライン編集は**自動保存**で、共通トップバーの保存ボタンは出さない＝**失敗を伝える担い手はこの画面だけ**。黙って落とすと「閉じても消えない」が破れる〔ADR-0026④〕。同じ理由で失敗し続ける間ディスクを叩き直しても直らないので**利用者に返す**／画面を移ると編集は失われるので**黙って捨てず聞く**（#719＝**サイドバーなど別の入口も同じ関門を通る**・`navigationGuard`）） |
| `TIMELINE_EDIT_OVERLAP` | warning | — | その場所には先に置いてある部品があります。ずらすか、列を足して重ねてください | `ADR-0032`・`11 §8 V24` |
| `TIMELINE_EDIT_TRACK_KIND` | warning | — | 音の部品は音の列に、絵や文字の部品は映像の列に置いてください | `ADR-0032`・`11 §8 V23` |
| `TIMELINE_EDIT_LOCKED` | warning | — | この列は固定されています。動かすには固定を外してください | `ADR-0032`・`11 §7.6`。⚠️ **締めの文はしようとしたことで変わる**＝**動かす**はこの共有コードの文（`editBlockedMessage`）、**中身を変える／削除する**は `lockedTrackMessage`（`content`/`delete` の2つだけ＝到達しない文言は持たない）。**新しいコードを足さない**（#844-4）＝出どころが増えても状態は同じ「列が固定されている」なので、この1行で受ける。出どころ＝押せない見た目の説明（`lockedHint`）／**二度押しで中へ入れないとき**（#832・ドリルイン）ほか。⚠️ ドリルインの断りだけは**画面ローカル**（`drillBlockedNotice`）で出す＝`clearSelection` の `CLEARED_NOTICES` が同じ押下の中で `editBlocked` を消してしまうため。**黙って飲み込まず**、従来どおり選択を解いたうえで理由を出す（`06 §12.1`） |
| `TIMELINE_EDIT_LOCKED_SELECTION` | warning | — | 固定された列の部品が選ばれています。固定を外すか、選び直してください | `ADR-0032`・#752-3（`locked` と分けるのは**次の行動が違う**ため＝選び直しでも進める。出るのは**消す**（`removeSelectedClipsChecked`＝1つでも「選んだもの」を通るので件数に依らず）と**まとめて動かす**（`moveClips`）。⚠️ **単体で動かす**（`moveClip`）**だけは `locked`**＝そちらは指す先が1つの列に決まる（#773 レビュー）。ボタンの説明とキーの断り文が**同じ文**を読む） |
| `TIMELINE_EDIT_GROUP_ACROSS_TRACKS` | warning | — | この列の部品が、ほかの列の部品とまとまりになっています。まとまりを外してから複製してください | `ADR-0032`・#767（列の複製＝**中身ごと**運ぶので、まとまりが列をまたぐと**片側だけ複製した方の絵が変わる**。黙って違う絵を作らない＝ADR-0026④） |
| `TIMELINE_EDIT_HIDDEN_TRACK` | warning | — | この列は「出さない」設定なので、置いても動画に出ません。ほかの列へ置くか、列の「⋮」から「動画に出す」を選んでください | `#684`・`ADR-0034` 決定10（**固定〔動かせない〕とは別**＝映らない。ボタンで置くときも隠した列は選ばないので、ドラッグでも同じにする〔ADR-0026②〕） |
| `TIMELINE_EDIT_NOT_FOUND` | warning | — | その部品は見つかりませんでした。選び直してください | `ADR-0032` |
| `TIMELINE_EDIT_NOT_AUDIO` | warning | — | その部品は音を持っていません。音の設定は、音や読み上げの部品で変えてください | `#724`・`11 §7.6.3.2`（音の設定＝速さ・使い始め・音量・フェードを**音を持たない部品**へ書こうとした。⚠️ `NOT_FOUND` で断らない＝「見つかりませんでした」は嘘になり、選び直しても直らない。**兄弟との使い分け**＝`VOLUME_POINTS_KIND` は音量の変化（点）専用／`CONTENT_FIELD` は「音はあるが**その項目が無い**」〔読み上げに「鳴らす音の選び直し」は無い〕／こちらは「**そもそも音を持たない**」。⚠️ 現状 UI は欄を出し分けているので到達しない＝**防御としてのみ存在する**〔`VOLUME_POINTS_KIND` と同型〕） |
| `TIMELINE_EDIT_NO_ORIGINAL_AUDIO` | warning | — | この動画には音が入っていないので、元の音は鳴らせません。音を付けるなら、音の列に音を置いてください | `#512` 段2・`11 §7.6.3.2`（動画の**元の音**を、動画ではない部品か**音の入っていない動画**へ書こうとした。⚠️ `NOT_AUDIO` と分ける＝「音や読み上げの部品で変えてください」は動画の話をしていないので、従っても直らない。⚠️ 画面は `canUseOriginalAudio` で欄を出し分けるので通常は到達しない＝**防御としてのみ存在する**） |
| `TIMELINE_EDIT_EXPORTING` | warning | — | いま動画を書き出しています。終わってから編集してください | `ADR-0032`・`11 §7.6.5`（書き出しは**始めた時点の文書**を焼く＝入らない編集を受け付けない・ADR-0026①） |
| `TIMELINE_EDIT_ORIENTATION` | warning | — | この見た目パターンは向き（横長・縦長）がこの動画と違うので置けません。同じ向きのものを選んでください | `ADR-0032`・`11 §7.6.3`（層の座標がそのまま使われて画面外へ出る＝置く前に断る） |
| `TIMELINE_EDIT_EXPLODE_ANCHOR` | warning | — | 動き（拡大・回転）が付いた部品は、そのままバラすと絵がずれます。動きを外してからバラしてください | `ADR-0032` 決定23・`11 §7.6.3`（動きの支点がクリップの箱→メンバーの外接矩形へ変わる＝はみ出す中身があるとずれる） |
| `TIMELINE_EDIT_EXPLODE_TRIM_END` | warning | — | 切り出す終わりを決めた動画が入っています。そのままバラすと流れる長さが変わります。その枠に切り出していない動画を入れ直すか、バラさずに使ってください | `#512` 段3b・ADR-0032 決定23（直接置きの語彙に「ここまで」が無い＝置いた長さを縮めると**絵が早く消え**、縮めないと**その先まで流れる**。どちらも「前後で絵が変わらない」に反するので、動きが付いた部品〔`EXPLODE_ANCHOR`〕と同じ流儀で先に断る） |
| `TIMELINE_EDIT_EXPLODE_BACKGROUND_VIDEO` | warning | — | 差し込み口ではない場所（背景など）に動画が入っています。そのままバラすと動き出して見た目が変わります。その動画を差し込み口へ入れるか、写真に差し替えてからバラしてください | `#816-4`・ADR-0032 決定23（差し込み口でない層の動画は**静止画として描かれる**のに、バラすと直接置きになり**実映像として動き出す**＝前後で絵が変わる。「動かさない」を写す語彙が直接置きに無いので `EXPLODE_TRIM_END` と同じ流儀で先に断る。⚠️ **立ち絵は対象外**＝下記 `EXPLODE_CHARACTER_VIDEO` へ分けた） |
| ~~`TIMELINE_EDIT_EXPLODE_CHARACTER_VIDEO`~~ | ～～**退役**（#809・2026-08-27）～～ | — | ～～立ち絵として入れた動画は、まだバラせません…～～ | ⚠️ **自分で消えた断り**＝この関門の理由は「**バラすと動き出す**（バラす前は静止＋書き出しも断っていた）」だったが、#809 で**バラす前から動く**ようになったので前提が消えた。判定は `videoPlacementsOfClip` から作る `placementByLayer` を見ており、立ち絵が置き場所になった時点で**自動的に成立しなくなる**（条件を2か所に書いていなかったので、直す作業も要らなかった）。⚠️ **文言と定義は消さず退役として残す** |
| `TIMELINE_EDIT_SPLIT_PAST_SOURCE` | warning | — | そこは動画を使い切った後なので分けられません。動画が流れている間（映像が止まる前）の位置で分けてください | `#816` レビュー・ADR-0026①（後半の頭出しがそこまで進むと**切り出す終わりを追い越して反転レンジ**になり、終端が「無し」へ正規化されて**切り捨てたはずの先が流れ出す**〔元の音が入っていれば鳴り出す〕＝分ける前は最後のコマで凍っていたので、切っただけで絵が変わる。**素材の実尺を越える場合は書き出しが理由なく落ちる**ので同じ門で止める。判る材料が無いときは断らない） |
| `TIMELINE_EDIT_EXPLODE_TRIM_END_PER_USE` | warning | — | この枠だけ切り出す終わりを決めた動画が入っています。そのままバラすと流れる長さが変わります。その枠の動画をいったん「なし」にして入れ直してからバラしてください | `#512` 段3b・ADR-0034 決定5（**出どころで案内を分ける**＝「ここまで」は**その枠だけの設定**〔`slotClips`〕と**素材の既定**〔`asset.clip`〕のどちらからも来る。解決は per-use が優先なので、前者に「素材の画面で外して」と案内すると**従っても解除されない行き止まり**になる。入れ直すと `setClipAssetRef` がその枠の設定を落とす） |
| `TIMELINE_EDIT_LINKED_SUBTITLE` | warning | — | 連動している字幕を置ける場所がありません。字幕をほかの列へ移すか、連動をやめてください | `ADR-0032` 決定24（読み上げ側の操作を断る理由＝触っていない列の話にしない） |
| `TIMELINE_EDIT_CURVED_EASING` | warning | — | この動き方は途中で分けられません。「動き」の欄に出ている秒数の位置か、動きの付いていない所で分けてください | `ADR-0034` 決定16・`11 §7.6.3`（**#753 で範囲が狭まった**＝カーブは前後へ切り分けて焼けるようになったので、断るのは**その形が表せない／置けない値になるとき**だけ＝「両端ゆっくり」〔3次ベジェで表せない〕／切れ目で進み具合が端に着く形／ハンドルが交差したカーブ〔切ると時間の制御点が 0〜1 を外れる〕／プロパティごとに別のカーブが要る／書き直す相手が後半だけの区間の行き先でもある／切れ目に焼く値が置ける範囲の外〔行き過ぎて戻るカーブ〕。⚠️ **近い形で黙って置き換えない**〔#262 と同じ流儀・ADR-0026④〕。文言の「動き方」は画面の「ここまでの動き方」と同じ語） |
| `TIMELINE_PLAY_EXPORTING` | warning | — | いま動画を書き出しています。終わってから再生できます | `ADR-0032`・#752-6（`TIMELINE_EDIT_EXPORTING` と分けるのは**次の行動が違う**ため＝こちらは「再生」。ボタンの説明とキーの断り文が**同じ文**を読む） |
| `TIMELINE_EDIT_PLAYING` | warning | — | 再生を止めてから使えます | `ADR-0032` 決定21（位置を使う操作は再生中に押させない・理由を出す） |
| `TIMELINE_EDIT_UNSPLITTABLE` | warning | — | 読み上げと、それに合わせている字幕は分けられません（文と音がずれるため）。字幕だけ分けたいときは「連動する読み上げ」で連動をやめてください | `ADR-0034` 決定16 |
| `TIMELINE_EDIT_SPLIT_OUTSIDE` | warning | — | その位置では分けられません。再生位置を部品の中（両側が0.1秒以上残る所）へ動かしてください | `ADR-0034` 決定16 |
| `TIMELINE_EDIT_LINKED_SUBTITLE_TIME` | warning | — | 連動している字幕の時間は読み上げに合わせています。連動をやめると自分で動かせます | `ADR-0032` 決定24（区間の一致を保つ） |
| `TIMELINE_EDIT_VOLUME_POINTS_FULL` | warning | — | 音量の変化は1つの部品に60か所までです。ほかの点を外してから置いてください | `ADR-0032` 追補・`11 §7.6.3.2`（上限は `VOLUME_POINTS_MAX`＝FFmpeg の式の解析上限。**置けたのに書き出しで断られる、を作らない**＝`§7.6.5` と対） |
| `TIMELINE_EDIT_VOLUME_POINTS_KIND` | warning | — | 音量の変化を置けるのは、音や読み上げの部品だけです。音の部品を選び直してください | `ADR-0032` 追補・`11 §7.6.3.2`（鳴る音を持たない部品に意味の無いデータを書かない） |
| `TIMELINE_EDIT_CONTENT_FIELD` | warning | — | この部品にはその項目がありません。直したい部品を選び直してください | `#684` レビュー・`11 §7.6.3`（`setVisualClipContent`＝その種類が持たない項目／`setClipBox`＝箱を持てない部品〔見た目パターン・音・読み上げ〕・#685。**列の種別違い〔V23〕とは別**＝「列に置き直してください」は項目違いには当たらない案内になる。`VOLUME_POINTS_KIND` と同じ流儀） |
| `TIMELINE_AUDIO_SOURCE_MISSING` | warning | — | 音が見つからない部品があります。その部品は鳴りません。読み上げを作り直すか、音を選び直してください | `ADR-0032`・`11 §7.6.2.2` |
| `TIMELINE_TEMPLATE_NOT_FOUND` | warning | — | 見た目パターンが見つからない部品が3個あります。その部品は動画に出ません。その部品を消して、置き直してください（**件数は可変**。**選んでいる1つの詳しい欄では**「この部品の見た目パターンが見つかりません。…」＝#834-2） | `ADR-0032`・`11 §7.6.4`（描かれないものを黙らせない。⚠️ **文言の単一の参照元は `missingTemplateMessage`**〔#834-2〕＝以前は画面2か所に手書きされ、禁止語の検査の走査対象の外に落ちていた〔`lockedTrackMessage` と同じ流儀＝件数の有無で締めを変える〕） |
| `TIMELINE_EXPORT_EMPTY` | warning | — | まだ何も置かれていないので、動画を書き出せません。素材や文字を置いてから書き出してください | `ADR-0032`・`11 §7.6.5`（尺 0＝呼び出し側で止める） |
| ~~`TIMELINE_EXPORT_VIDEO_ASSET_UNSUPPORTED`~~ | ～～**退役**（#809・2026-08-27）～～ | — | ～～立ち絵として入れた動画は、まだ書き出せません…～～ | **立ち絵に入れた動画も映って元の音も鳴るようになった**ので、断る理由が無くなった（#512 の段1〜段3b と同じく `videoPlacementsOfClip` が置き場所として数える）。⚠️ **文言と定義は消さず退役として残す**（#635 の `TIMELINE_OVERLAY_RETIRED` と同じ流儀＝過去の版の記述と突き合わせられる）。⚠️ **見た目パターンが解けないときは置き場所にならない**が、そちらは `TIMELINE_EXPORT_TEMPLATE_UNRESOLVED` が別に断る（二重に見ない） |
| `TIMELINE_VIDEO_AUDIO_UNKNOWN` | info | — | この動画に音が入っているか確かめられませんでした。もう一度取り込むと使えることがあります | `#512` 段2・段3b（取り込みのときに動画を調べられなかった素材＝**「入っていません」と断定しない**。断定すると嘘の理由になり、次の行動も誤る〔実際は取り込み直し〕。場面形式も同じ2文で出し分けている＝ADR-0026②） |
| `TIMELINE_VIDEO_NO_AUDIO` | info | — | この動画には音が入っていません（音を付けるなら、音の列に音を置いてください） | `#512` 段2・段3b（元の音の欄は**音が入っている動画にだけ**出す＝直接置きも差し込み口も同じ2文＝出しておいて効かない、を作らない。入っていないものには**その場で次の行動**を出す・§2-5。段1 の `TIMELINE_VIDEO_AUDIO_PENDING` は元の音が鳴るようになったので置き換え） |
| `TIMELINE_VIDEO_STILL_IN_GROUP_FADE` | info | — | まとまり全体を薄くしている間は、ここでは動かずに見えます（書き出した動画では動きます） | `#512` 段1・`11 §7.6.4`（**合成の単位が跨るときは実映像を出さない**＝層ごとに薄さを掛けると重なった所で下が透け、書き出し〔1枚にしてから掛ける〕と別の絵になる。**黙って静止画に見せない**・§2-5） |
| `TIMELINE_VIDEO_STILL_ROTATED_CROP` | info | — | 回した部品を切り抜いている間は、ここでは動かずに見えます（書き出した動画では動きます） | `#512` 段1・`11 §7.6.4.1`（**切り抜きを回す中心が食い違う**＝書き出しは矩形自身の中心・画面は部品の中心で回るので**別の窓**になる。直せるまでは出さない側へ倒す。⚠️ **中心対称な切り抜きは断らない**〔過剰に止めない〕） |
| `TIMELINE_VIDEO_STILL_UNPLAYABLE` | info | — | この動画は、ここでは映像も音も出せません（形式が合わないか、ファイルが見つかりません）。MP4 で取り込み直すと、ここでも確かめられます | `#816-1`・ADR-0001（**この画面が復号できない形式**〔`.avi`/`.mkv` 等〕＝取り込めるが再生できず、**例外ではなく主要ケース**。以前は静止＋無音になるだけで理由も出ず、書き出しは実映像＋元の音＝**見えていたものと違う動画**が成功として出ていた。音も鳴らせない〔同じ復号器を通る〕ので、鳴らす側からも外して理由だけ出す。⚠️ **素材のファイルが見つからない**ときも同じ状態に落ちる〔失敗の合図は理由を区別しない〕＝**「書き出しには入る」と約束しない**・両方に当たる行動〔取り込み直す〕だけを出す） |
| `TIMELINE_EXPORT_TEMPLATE_UNRESOLVED` | warning | — | 見た目パターンが見つからない部品があります。そのままでは動画に出ません。その部品を消して、置き直してください | `ADR-0032`・`11 §7.6.5`（描かれないものを黙って落とした動画を成功にしない＝ADR-0026④・場面形式の書き出し停止と同じ） |
| `TIMELINE_EXPORT_SUBTITLE_LINK_BROKEN` | warning | — | 連動する読み上げが見つからない字幕があります。そのままでは動画に出ません。連動先を選び直すか、字幕の文を入れてください | `ADR-0032` 決定24・`11 §7.6.2.3`（**指しているのに見つからない**もの限定。「何も描かれない字幕」全部には広げない＝焼き出しが作る「元から空の字幕の箱」まで止め、焼いた直後の動画が書き出せなくなる＝#787 で試して取り下げ） |
| `TIMELINE_EXPORT_VOLUME_POINTS_TOO_MANY` | warning | — | 音量の変化の点が多すぎる部品があります。1つの部品に置けるのは60個までです。いらない点を外すか、部品を分けてください（**分けられる部品が無いときは**「音量の変化の点が多すぎる部品があります。1つの部品に置けるのは60個までです。いらない点を外してください」＝#831） | `ADR-0032` 追補・`11 §7.6.5`（点の数だけ式の項が増え、**点 95 個までは通り 96 個で FFmpeg が式を解析できなくなる**＝実測。渡してから落ちると「もう一度お試しください」しか出せないので**押す前に断る**・上限は `VOLUME_POINTS_MAX`。⚠️ **`#831`＝「分けてください」は読み上げには実行できない**〔読み上げは分けられない〕＝挙げた部品に分けられる種類が1つでもあるときだけ添える。`exportBlockedMessage` の Record には無く `volumePointsTooManyMessage(hasSplittable)` が締めを変える〔`lockedTrackMessage` と同じ流儀〕） |
| `TIMELINE_EXPORT_ASSET_UNREADABLE` | warning | — | 素材のファイルを読めませんでした。そのままでは動画にその絵が出ません。素材を取り込み直すか、その部品を置き直してください | `#716` レビュー・`11 §7.6.5`（**プレビューでは開いた時点の表示先で写真が出たまま**なので、断らないと「見えていたものと違う動画」が成功として出る＝ADR-0026④。見た目未解決〔`TEMPLATE_UNRESOLVED`〕と同じ流儀） |
| `EXPORT_OTHER_RUNNING` | warning | — | ほかの動画を書き出しています。終わってから、もう一度お試しください | `ADR-0032`・`11 §7.6.5`（場面形式とタイムライン形式は一時ファイルの置き場を共有＝同時に走らせない）。⚠️ **相手が後片づけ中のときもこの文で受ける**（差分再監査 ℹ️）＝厳密には相手も走ってはいないが、**待つ相手が自分の外にある**点は同じで、次の行動も変わらない。到達も極めて狭い（相手の片づけ中に別形式を開いて書き出し画面まで行く）。自分の片づけ待ちだけを `EXPORT_CLEANUP_PENDING` に分けるのは、そちらが**成功のたびに必ず出る**＝主語のずれが毎回目に入るため |
| `EXPORT_CLEANUP_PENDING` | warning | — | 前の書き出しの片づけをしています。終わると、また書き出せます | `ADR-0032` 決定22・`11 §7.6.5`（#843）＝**書き出しの終わり（成功・中止・失敗）は片づけより先に立つ**ので、片づけ（一時ファイルの削除＝フレーム数に比例）の間はボタンが戻っているのに締めが返っていない。⚠️ **`EXPORT_OTHER_RUNNING` を流用しない**＝走っている「ほかの動画」は無く、片づけているのは**自分の直前の回**（主語が実態と違う案内にしない）。⚠️ **押す前に無効化して理由を出す**（`06 §12.1`）＝押す前の理由と押したときの理由は同じ述語（`isOwnCleanupPending`）から採る。**両形式に同じ窓がある**ので同じ扱いにする（ADR-0026②）。⚠️ **「お試しください」と書かない**（差分再監査② 🟡2）＝この文は押そうとしたときだけでなく**書き出しが成功した直後にも必ず出る**（終わりの合図が締めの返却より先）。片づけるコマは尺×fps 個あるので、成功のたびに数秒「動画を書き出しました」の**上**に警告として並び、何も頼んでいない利用者が催促を読まされる＝**成功が失敗に見える**。だから**状態の文**にする（何が起きていて、いつまた押せるか）＝押したときの断りとしても成り立つ |

> ⚠️ **この表の「利用者に出す文言」は機械で守る**（#750 再レビュー）＝`uiLabels.test.ts` が
> `editBlockedMessage`／`exportBlockedMessage`／`bakeNoteMessage` の各表**と、共有関数が返す文**
> （`lockedTrackMessage`／`hiddenTrackDuplicateMessage`／`volumePointsTooManyMessage`／
> `missingTemplateMessage`）を走査し、§2-3 の禁止語（キーフレーム・JSON・スキーマ等）が
> 混ざっていないか確かめる。**目視のレビューで繰り返し見つかっている**ので、1つ直すのでなく
> 一覧をまとめて見る。
> ⚠️ **画面に手書きしない**（#819-2・#834-2）＝手書きの文はこの走査の**外**に落ちるので、
> 混ざっても誰も気づかない（実際 `TIMELINE_TEMPLATE_NOT_FOUND` が画面2か所に手書きされていた）。
> 締めが状況で変わる文は**共有関数**にして、上の走査対象へ必ず足す。
>
> ⚠️ **表の文言と実装の文字列が一致していることも機械で守る**（#855・`errorStateTable.test.ts`）＝
> 上の禁止語の走査は**言葉の中身**しか見ておらず、**表と実装がずれていても緑のまま通っていた**。
> **目視では6回すり抜けている**（直近は PR #854＝「正典と実装のズレを消す」PR 自身が同じズレを作った）。
> 見るのは2つの層＝**（あ）文言そのもの**＝**①一致するか**（片方だけ直したら落ちる・**どちらが新しいかは
> 機械には分からない**ので両方を並べて出す）／**②実装にある文言が表にもあるか**（足したのに正典へ
> 書き忘れたら落ちる）。**（い）読み取りの健全性を守る群**＝**③表の行を1つも取りこぼしていない**
>（書き方のゆれた行を厳密な判定が拾えないと**検査の外に落ちる**。⚠️ **下限のしきい値では足りない**＝
> 1〜2行こぼれても件数は割らない）／**④どの行も列が5つ**（セル内に `|` が入ると**別の列を文言として読む**。
> ⚠️ **件数は減らないので③でも気づけない**）／**⑤守れている件数が黙って減っていないか**。
> ⚠️ **（い）を足すときはこの括りへ**（PR #862 レビュー ℹ️2・#513 の判断ログ）＝走査そのものが
> 壊れたことに気づけない、が（あ）の空振りを生む。
>
> ℹ️ **突き合わせは「文末の『。』を落として等値」**（表は落とす流儀・実装は付ける）。
> **締めが状況で変わる文**（`lockedTrackMessage` ほか上記4つ）は**1つの行に対して複数の文**を返すので
> 等値では守れず、この検査の**対象外**（禁止語の走査は引き続き見ている＝守り方が違うだけ）。

---

## 7. ログ

- **ユーザー表示（次の行動）と技術ログを分離**。技術詳細は詳細モード／ログファイルへ。
- 書き出し失敗時はFFmpegのコマンドと標準エラーをログ保存（`05 §6,§17`）。
- AI送受信は `ai/history`（`12`）。**APIキー・秘密情報・元動画ファイルはログに残さない**（`13 §7`）。

---

## 8. リカバリUX

- **生成中でも完了したシーンから確認できる**（`06 §17`）。長尺はパート単位で確認。
- 失敗時の導線：**再試行 / 手動作成 / 前回AI結果の復元**（`12 §9.3`）。
- **失敗の導線は生成中の画面に閉じない**（#590）。生成が `failed` のまま他の画面（たたき台／公開前チェック／
  仕上がり確認／書き出し）へ移っても、**どの画面でも理由と同じ2択（再試行／手動作成）を出す**。
  場面が0件なのは同じでも「まだ作っていない」と「作ろうとして失敗した」は別の状態で、後者を
  「まだ場面がありません」とだけ言うと原因も次の行動も分からない（`§2-5`）。
- **「場面が1つも無い」ときの表示は4画面で共有する**（#590・ADR-0026②）。見分けは
  **未作成（`idle`）／作成中（`generating`）／失敗（`error`）／場面0（`ready`）**の4状態で、
  次の行動は**場面を作れる画面（たたき台）へ送る**か、その画面自身が作れるなら**その場で場面を追加**。
  0件でも会社情報・素材は残っているので、空状態から**今の内容を捨てる**導線（新規作成）は
  未作成（`idle`）のときだけに限る。
- 自動補正の結果は `Scene.warnings[]` に蓄積し、件数＋対応内容を非技術語で提示（`01 §6.7`）。
