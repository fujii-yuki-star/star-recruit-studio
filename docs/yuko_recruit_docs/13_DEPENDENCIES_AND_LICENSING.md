# 13. 依存・ライセンス・配布仕様

> ⚠️ **本書は 2026-06-10 時点の調査に基づく整理であり、法的助言ではない。** 各ライセンス・規約は変更されうるため、**配布（特に商用）前に最新の公式規約と、必要に応じて専門家の確認**を行うこと。確認すべき項目は §9 のチェックリストに集約する。
> 本書は Phase 0（技術検証）の前提を確定するための資料（`02_MVP_ROADMAP.md`・レビュー論点⑦）。

---

## 1. 目的

本ソフトは外部の実行バイナリ・音声エンジン・フォント・素材に依存する。これらの**同梱可否・ライセンス・クレジット表記義務・APIキーの扱い**を整理し、実装と配布の前提を定める。

---

## 2. 依存一覧（サマリ）

| 依存 | 用途 | ライセンス（執筆時点の理解） | 配布上の要点 | 状態 |
|---|---|---|---|---|
| FFmpeg | 動画合成・エンコード | LGPL 2.1+（`--enable-gpl` 不使用時）。GPL部分を使うと全体GPL | LGPLビルド＋動的リンク＋ソース提供。H.264特許は別途 | §3 / 要決定 |
| VOICEVOX | ゆうこの声（音声合成） | ソフト本体規約＋**各キャラ規約は別**（キャラ規約優先） | **「VOICEVOX:ずんだもん」クレジット必須** | §4 / 要確認 |
| ずんだもん（音源） | 既定の声 | 東北ずん子・ずんだもんプロジェクト（SSS LLC.）規約 | 商用可・クレジットで利用可。**別キャラの声として使う点は要確認** | §5 / 要確認（固有リスク） |
| 日本語フォント | テキスト・字幕描画 | 同梱・埋め込み可のものを選定（例：SIL OFL系） | アプリ同梱＋ライセンス表記 | §6 / 要決定 |
| 外部AI API | 動画構成案生成 | 各社規約（OpenAI/Claude/Gemini） | APIキーはユーザー管理。鍵をAI本文に載せない | §7 |
| 標準BGM | 同梱BGM | 再配布・商用採用動画利用が可能なもののみ | 出所とライセンスの記録 | §8 / 要決定 |
| 標準装飾アセット | パネル・帯等 | 同上 | 同上 | §8 / 要決定 |
| ゆうこ素材 | マスコット立ち絵 | 自社が商用利用＋アプリ同梱の権利を保有 | 権利の明確化 | §8 / 要決定 |

---

## 3. FFmpeg

### 事実（執筆時点）
- FFmpeg は **LGPL 2.1+**。ただし `--enable-gpl` / `--enable-nonfree` を付けると **GPL/非フリー** 扱いになり、アプリ全体に強い義務が及ぶ。
- LGPL遵守の定石：**`--enable-gpl` を付けずにビルド**、FFmpegライブラリは**動的リンク**、配布バイナリに対応する**FFmpegのソースを提供**（同梱 or 入手手段の明示）。
- **H.264 はFFmpegのライセンスとは別に特許**の論点がある。H.264エンコード/配布には特許ライセンス（MPEG-LA系）が関係しうる。Cisco の **OpenH264**（`--enable-libopenh264`）は Cisco が実行時取得分の特許料を負担する方式で、LGPLビルドと両立し商用回避策になりうる。

### 決定（[`adr/0002`](adr/0002-ffmpeg-codec.md)・Accepted）

> H.264は **OpenH264採用**（Cisco配布バイナリでAVC特許料をカバー＝**自前の特許ライセンス不要**）。FFmpegは **LGPLビルド＋動的リンク**。エンコーダは概ね Constrained Baseline（採用動画には十分）。

> **更新（2026-06-18・[`adr/0013`](adr/0013-h264-via-media-foundation.md) / PR#116）**: H.264 の**主経路は Media Foundation（`h264_mf`・OS提供）**へ変更（実機スパイクで確証）。**OpenH264 はフォールバック**に降格。以下の OpenH264 関連の遵守事項・必須クレジットは「フォールバックを採用する場合」に適用される。MF 主経路では Cisco 必須クレジットは発生しない（FFmpeg LGPL 表記は両経路で共通）。

遵守事項：
1. **LGPLビルド＋動的リンク**で同梱（GPLオプションを使わない）。
2. 配布物に **FFmpegのソース入手手段** と **LGPLライセンス文** を含める（§の「クレジット/ライセンス画面」）。
3. H.264 は **OpenH264 採用** か **特許ライセンス取得** のいずれかを選ぶ。MVPの動作検証は手元ビルドで可だが、**配布前に方式を確定**。
4. FFmpeg は `infrastructure/ffmpeg` 越しに呼ぶ（`CLAUDE.md §4`）。バージョンを固定し記録する。

### 未決定
- 同梱FFmpegの**正確なビルド構成**（有効化するエンコーダ／OpenH264の採否）。
- 配布形態（インストーラ同梱 / 初回DL）。

---

## 4. VOICEVOX

### 事実（執筆時点）
- 商用・非商用ともに利用可能。**クレジット表記が必須**（基本形式「**VOICEVOX:キャラクター名**」、例「VOICEVOX:ずんだもん」）。アプリの場合は**紹介・アバウト画面等に記載**。
- **VOICEVOX（ソフト）の規約と、各キャラクター（権利者）の規約は別物**で、使うキャラの規約が優先。
- クレジット表記をしない商用利用は、VOICEVOX側で**1キャラ毎に有償契約**（執筆時点で40万円＋税の案内あり）。

### 推奨
1. アプリ内に **クレジット/ライセンス画面**を必ず設け、`VOICEVOX:ずんだもん`（および使用キャラ全て）を表示。書き出し設定にも任意でクレジット焼き込みオプションを検討。
2. VOICEVOX エンジンは **同梱し、アプリ起動時に自動起動**する（[`adr/0005`](adr/0005-voicevox-bundling.md) で決定。同梱・再配布条件は確認済み）。実装は `infrastructure/voicevox` 越しの**接続（ローカルエンジン）**で進め、`VoiceProvider` で抽象化（`CLAUDE.md §4`）。接続先設定は別ポート/別PC向けの上級者用フォールバックとして残す。
3. バージョン・接続先を設定画面に持つ（`06_UI_SPEC.md §14`・`14.2`）。

### 決定（[`adr/0005`](adr/0005-voicevox-bundling.md)・§5）
- ✅ エンジンを**同梱＋アプリ起動時に自動起動**（接続先設定はフォールバック）。同梱・再配布条件は確認済み。
- ✅ 既定キャラ＝**ずんだもん**（ナレーター用途・§5 / `adr/0003`）。
- 残（実装・配布の詳細）: 同梱ビルド選定（CPU/GPU・サイズ）／プロセス管理（ポート競合・起動待ち・終了）／配布時の最終法務・バージョン固定（ADR-0005 未解決論点）。

---

## 5. ずんだもん（音源）と「ゆうこ」の関係

> ✅ **決定（[`adr/0003`](adr/0003-narration-voice.md)）**：ずんだもんは **ナレーター音声（VOICEVOX:ずんだもん）** として用いる。ゆうこ（自社保有マスコット）の“固有の声”とは称さない。これにより「別キャラの声を当てる／なりすまし」の懸念は解消し、**標準的なVOICEVOX利用**に整理された。残るは標準コンプライアンス（**常時クレジット表示**・原作者の承認を装わない・規約通読）。

本製品は **画面上のマスコットが「ゆうこ」**（自社保有）で、**ナレーション音声が「ずんだもん」**（VOICEVOX）という構成。以下は背景の整理。

### 事実（執筆時点）
- ずんだもん等の音源は**商用利用可・クレジットで利用可**（東北ずん子・ずんだもんプロジェクト／SSS LLC.）。著作権表記(c)は必須ではないが、**VOICEVOXのクレジットは必要**。
- 企業利用も一定条件下で可能とされるが、**商業出版等は別途ライセンス**が必要になる場合がある。

### 確認が必要な点（リリース前必須）
1. **ずんだもんの音声を、別名のキャラクター「ゆうこ」の声として用いる**ことの可否・表記方法。音源規約（Voiceger ずんだもん音源利用規約）と VOICEVOX 規約で、キャラの取り違え・誤認を招く表現が禁じられていないかを確認する。
2. ずんだもんの**イラスト/キャラクター自体は使用しない**（表示は「ゆうこ」）ため、キャラクター画像のライセンスは原則対象外。ただし「ずんだもんの声＝ゆうこ」という訴求が**誤認・なりすまし**に当たらないか要確認。
3. 顧客（採用企業）が作る最終動画も商用物。**エンドユーザーのクレジット表記運用**（アプリが自動付与 or 手順案内）を設計する。

### 推奨
- 上記1が**グレー/不可**の場合の代替を用意：
  - (a) 既定の声を、別キャラとして使うことが明確に許容される音源へ変更。
  - (b) ゆうこ専用の独自ボイス（外部TTS等）を将来用意（`01 §8.3`）。
- いずれにせよ **クレジット自動表示**を実装要件とする。

---

## 6. フォント

### 方針
- 日本語の**テキスト・字幕描画はWeb描画**（ADR-0001）で行うため、**アプリ同梱フォントを preview/export で同一指定**する必要がある（パリティのため）。
- **判定基準は「広く配布されているか」ではなく、ライセンスが“同梱・再配布・埋め込み”を許可しているか**。推奨は **SIL Open Font License (OFL) 1.1** 系（例：Noto Sans JP、M PLUS。広く使われ再配布可）。
- ⚠️ **Windows標準フォント（游ゴシック・メイリオ等）は同梱・再配布不可**。パリティ用に同梱するフォントには使わない（OS依存にすると環境差でパリティが崩れる）。

### 未決定
- 最終的なフォント選定（本文用・見出し用）。ライセンス本文を配布物に同梱。

---

## 7. 外部AI APIキーの取り扱い（セキュリティ）

### 要件
- APIキーは **OSの安全な保管領域**（Windows資格情報マネージャ／OSキーチェーン、Tauri の keyring 系）に保存する。**`project.json` や設定ファイルへ平文保存しない。**
- キーを **AIへ送るペイロード本文に含めない**（認証ヘッダのみ）。ログ・AI履歴（`ai/history`）にも残さない。
- リポジトリ・サンプル・fixtures に実キーを**コミットしない**（`.env.example` 等のプレースホルダのみ）。
- 「誰が鍵を管理するか（個人/社内共通）」「社内利用時の外部AI利用ルール」は運用方針として要決定（`01 §19`）。

---

## 8. 同梱素材（BGM・装飾・ゆうこ）

- **標準BGM・標準装飾アセット**：再配布可能かつ**商用の採用動画で利用可能**なライセンスのもののみ同梱（CC0 等、または自社制作）。各素材の**出所・ライセンス・改変可否を台帳化**する。
- **ゆうこ素材**：✅ **自社保有マスコット**につき、商用利用・アプリ同梱・エンドユーザー動画への利用権はクリア（`17`）。
- エンドユーザー持ち込み素材・採用動画に映る人物の**肖像権**は、公開前チェック（`01 §13`）とマニュアルで注意喚起。

---

## 9. 確認・決定チェックリスト

> 凡例: `[x]` 実装済/決定済 ・ `[~]` 部分実装（残タスクあり）・ `[ ]` 未実装/未確定。

### 決定済み（2026-06-10）
- [x] H.264特許対応 ＝ **OpenH264採用**（自前ライセンス不要、`adr/0002`）。残: 商用配布の最終法務スキム・Ciscoバイナリ取得方式
- [x] ずんだもん ＝ **ナレーター用途として整理**（`adr/0003`）。残: VOICEVOX/各キャラ規約の通読・常時クレジット実装
- [x] ゆうこ素材 ＝ **自社保有で権利クリア**
- [x] フォント ＝ **OFL系を同梱**（游ゴシック等は同梱不可）。残: 最終選定

### リリース前に残る確認（法務・公式規約）
- [~] FFmpeg：**取得方式決定（2026-06-18・ユーザー）**＝OpenH264 は**同梱せず初回 Cisco 取得**／dev は `ffmpeg-static` 継続／配布版は LGPL 必須。候補調査＝[`research/ffmpeg-openh264-windows.md`](research/ffmpeg-openh264-windows.md)（**推奨＝OpenH264 を動的参照する自前ビルド**。BtbN は OpenH264 を静的リンクのため不可）。残: バージョン pin・`-buildconf`/DLL依存 検証・ハッシュ固定。
- [x] VOICEVOX：エンジン**同梱＋自動起動**を決定（`adr/0005`）。**規約・同梱配布の可否はユーザー（事業側）確認済み（2026-06-18）**。残: 同梱ビルド／プロセス管理・バージョン固定。
- [ ] エンドユーザー動画のクレジット表記運用
- [ ] 標準BGM・装飾の入手元と権利台帳
- [ ] **🔴 OpenH264 ライセンス条件**（[research/ffmpeg-openh264-windows.md](research/ffmpeg-openh264-windows.md) §5）: 必須クレジット「**OpenH264 Video Codec provided by Cisco Systems, Inc.**」の表示／**商用 AVC コンテンツ配信の MPEG-LA 許諾要否**（顧客動画は商用＝法務確認）。

### 実装要件（コードに落とす） ※実装監査 2026-06-17
- [x] アプリ内「クレジット/ライセンス」画面 ＝ **実装済**（`AboutScreen`：VOICEVOX:ずんだもん／FFmpeg(LGPL 2.1+・**ソース入手先URL をクリック可能で表示**＝PR#113)／Noto Sans JP(OFL)）。残: ライセンス**本文**の配布物同梱、BGM/装飾を採用時に追記。
- [x] APIキーのOSキーチェーン保管 ＝ **実装済**（`infrastructure/aiClient` 経由で Rust keyring に保管・平文非保存・本文/ログ非混入＝ADR-0010 P1）。
- [~] FFmpeg/VOICEVOX/AI の `infrastructure` 越し呼び出し ＝ **実装済**（`ffmpegExport`／`voiceProviders/voicevoxProvider`／`aiProviders`）。残: **バージョン固定と記録**。
- [ ] 書き出し時のクレジット自動付与（任意ON/OFF）＝ `AboutScreen` にトグル UI のみで**焼き込み未実装・設定も揮発**＝backlog。

### Phase 0 で技術検証 ※実装監査 2026-06-17
- [~] LGPLビルドFFmpegで `overlay`/`xfade`/`amix` ＝ スパイクで **SVG→PNG→実MP4→連結を実証**（ADR-0001 スパイク・`ffmpegExport`）。残: **本番 LGPL＋OpenH264 ビルド**での確認（現状の検証ビルドは GPL/libx264＝spike 専用）。
- [ ] VOICEVOXローカルエンジンでWAV生成 ＝ **未**（現状 `MockVoiceProvider`・実エンジン疎通は backlog）。

### 次アクション（リリースに向けた切り分け・2026-06-17）

**コードで閉じられる（着手可）**
- ~~FFmpeg **ソース入手手段**の明示~~ ＝ **完了（PR#113＝クレジット/ライセンス画面に入手先URLをクリック可能で表示）**。配布物への LGPL **ライセンス本文**同梱・対象ビルドのソース提供は配布パッケージング/法務側。
- FFmpeg/VOICEVOX/AIモデルの **バージョン固定と記録**（`infrastructure` で定数化）。
- 書き出し時 **クレジット焼き込み**＋設定の永続化（backlog）。

**事業・法務の判断/作業が必要（コードでは閉じない）**
- VOICEVOX／東北ずん子・ずんだもんプロジェクトの **規約通読**（§5。ナレーター用途・常時クレジットの運用可否）。
- VOICEVOX **同梱ビルドの選定**（CPU/GPU・サイズ）と配布時の最終法務（`adr/0005`）。
- FFmpeg の **LGPLビルド構成確定**・Cisco OpenH264 バイナリ取得方式（`adr/0002`）。
- 標準BGM・装飾の **入手元と権利台帳**（CC0/自社制作）。
- 最終 **フォント選定**（OFL系・本文/見出し）とライセンス本文の同梱。
- エンドユーザー動画の **クレジット表記運用**（自動付与 or 手順案内）。

> 大物（**VOICEVOX 実疎通**・**実 FFmpeg 書き出し**）に着手する前に、上記「事業・法務」の該当項目を確定すること（本章の趣旨）。コード側の前提（infrastructure 抽象化・keyring・クレジット画面）は整っている。

---

## 10. 参考（2026-06-10 取得・要再確認）

- VOICEVOX ソフトウェア利用規約: https://voicevox.hiroshiba.jp/term/
- 東北ずん子・ずんだもんプロジェクト ガイドライン: https://zunko.jp/guideline.html ／ 音源規約: https://zunko.jp/con_ongen_kiyaku.html
- FFmpeg License and Legal Considerations: https://www.ffmpeg.org/legal.html

> これらは執筆時点の参照。**必ず最新版を確認**すること。
