# 出口監査レポート：α-7（配布品質）

- **凍結点**: `e01dd12`（develop・#968 が land した時点）
- **観点ファイル**: `docs/yuko_recruit_docs/audits/alpha7-final-audit-points.md`（最終更新 2026-09-02＝監査直前）
- **実施日**: 2026-09-02
- **機械層**: 緑（フロント 5252 tests・Rust 111 passed・EXIT=0）
- **やり方**: 7観点を並列で走らせ、🔴🟡 は**指摘した本人と別のエージェント**に反証させた（生存率で選別）

## 判定表

| 観点 | 判定 | 🔴 | 🟡 | ℹ️ |
|---|---|---|---|---|
| canon-schema | 要修正 | 0 | 1 | 2 |
| correctness | 要修正 | 1 | 4 | 2 |
| error-state | 要修正 | 1 | 5 | 2 |
| tests | 要修正 | 0 | 1 | 1 |
| ui-terms | 要修正 | 1 | 0 | 0 |
| resilience | 要修正 | 1 | 0 | 3 |
| parity | 合格 | 0 | 0 | 0 |

**敵対的検証**＝🔴 候補4件すべてに反証を試み、**4件とも CONFIRMED**（REFUTED は0）。

## 指摘（検証済み・重大度順）

### 🔴 戻した内容が、走っている保存に黙って上書きされる

`restorePointKeeper.ts:76` — 保存の直列化（`saveInFlight`）の外で `project.json` を書いていた。
`_doSave` は「まだ同じ文書か」を**書き込みの後**でしか見ないので、待たずに戻すと**戻す前の内容で上書き**される。
しかも画面の未保存確認は `saveStatus === "saving"` の間 false になるので**確認も出ずに素通り**する。
⚠️ `doRestorePoint` の説明文は**まさにこの上書き**を警戒していたが、防いでいたのは「**次の**保存」だけだった。
→ 直した（PR #971）。

### 🔴 声を作り直す知らせが、一度も描かれない

`HomeScreen.tsx:331` — 開き直すと**この画面が消える**のに、知らせをその**後**に置いていた＝
正常経路では**構造的に一度も出ない**。失敗経路では**読み込み失敗の理由を上書き**して「戻しました」＝
**失敗が成功に見える**。⚠️ テストは `onNavigate={vi.fn()}`（遷移しない）で緑のままだった。
→ 直した（開く前に知らせて、押してもらってから開く）。

### 🔴 案内が、実在しないボタン名を指していた

`uiLabels.ts` の `voicesClearedMessage` — 「その場面の『声を作り直す』から」と言うが、
作成前に戻したあとのボタンは**「声を作成」**（掛け合いには行ごとのボタン自体が無い）。
→ 場所（声の欄）で言う形に直した。

### 🔴 一覧に出ない動画は、復旧の導線に届かない

`lib.rs:304` — 戻す入口は**一覧の行からしか押せない**のに、`list_projects` は
`project.json` が読めない動画を**黙って飛ばして**いた＝**#263 が救おうとした場面がちょうど到達不能**。
→ **戻り先があるなら行を出す**（いまの内容 → 控え → いちばん新しい復元ポイント）。

### 🟡（すべて対応済み）

| 内容 | 直し方 |
|---|---|
| **素材の番号の使い回しで、別の絵になる**（⚠️ **通常の取り消しでも起きる**） | 既にある `reserveAssetId` へ場面形式も配線。**配線の漏れ自体も機械で守る** |
| **消した場面の声が食い違ったまま復活する**（読み上げの WAV は場面を消しても残る） | 比べようが無いので**作成前に倒す**。⚠️ **誤った前提をテストが固定していた**ので、テストも直した |
| **domain が出す断りが、表と機械で結ばれていなかった** | 走査を足したら**4件**漏れていた（`SCENE_TYPE_FALLBACK` は表に1行も無い） |
| **`SCHEMA_VERSION_UNSUPPORTED` が偽の緑** | 表の文はタイムライン形式のもの。**コメントに引用された同じ文**で検査が通っていた |
| **「読めない」を「まだ無い」に化かしていた** | `NotFound` だけ空とし、他は断る |
| **`one_line(detail)` が実装を通らないテストしか無い** | ⚠️ **未対応**（下記） |
| **タイムライン形式は復元ポイントを作らないのに、戻すボタンが出る** | ⚠️ **未対応**（下記） |

## 未対応（別 Issue へ）

- **`one_line(detail)` の検査が実装を通らない**（`trouble_log.rs`）＝`tag` 側は直したが `detail` 側が残っている
- **タイムライン形式に復元ポイントが無い**のに一覧のボタンは出る＝**来ない次の行動**を案内する
- **Rust の生の OS エラーが画面に出る**経路がある（`write_json_atomic` の失敗）
- **Escape の名簿は前に出ているものどうしを調停しない**（#965）
- **記録が1件ごとに同期で書く**＝警告が続く場面での引っかかりは実測が要る

## 実機チェックリスト（#352 へ）

- [ ] 復元ポイントが実際に増えていくか（5分に1つ・直近12個）
- [ ] 戻したあと、声の作り直しの案内が**開く前に**出るか
- [ ] 壊れた動画が一覧に出て、そこから戻せるか
- [ ] 記録のファイルが、失敗したときに実際に役立つ中身か
- [ ] MSI のクリーン環境インストール・Windows N

## この監査で分かったこと

⚠️ **「作った門番が効いていなかった」がこの期だけで6回**（括弧だけのセル／`Math.min` の頭打ち／
補助関数を直接見るテスト／遷移しないテスト／丸ごと差し替えたモック／`describe` の中の `afterEach`）。
**門番を作ったら、その門番を壊して赤くなるところまでやる**（変異チェック）が、この期でいちばん効いた。

⚠️ **「同じ規則が既にあるのに、片方にしか配線されていない」が2回**（`escapeOwners`／`reserveAssetId`）。
新しく作る前に**同じことをしている所を探す**。
