# ADR-0019: キーフレーム／場面内アニメのモデル（④・設計のみ・ADR-0001 を supersede）

- **状態**: **Accepted**（2026-07-02・α-4 実装着手＝利用者合意。設計は 2026-06-28 Proposed）— **実装は段階出荷**（下記）。**per-frame 実装が実際に landする PR で ADR-0001（場面内静止・単一フレーム）を Superseded（部分）へ更新**する。
- **日付**: 2026-06-28（Proposed）／2026-07-02（Accepted）
- **関連**: [`adr/0016`](0016-detailed-editing-completion-roadmap.md)（アンブレラ・本ADRは④）/ [`adr/0001`](0001-rendering-parity.md)（A2ハイブリッド・**選択肢C＝毎フレーム描画を将来オプションとして明記**・本ADRが supersede）/ [`adr/0004`](0004-rasterization-method.md)（WebView Canvas 単一ラスタライズ）/ [`adr/0018`](0018-cross-scene-timeline-model.md)（タイムライン2モデル・**キーフレームの格納先＝オーバーレイ**）/ [`adr/0009`](0009-scene-transitions.md)（場面間トランジション）/ `CLAUDE.md §10`（キーフレーム＝MVP外）・`§2-4` / `05_RENDERING_SPEC.md` / `11 §1`（schemaVersion）
- **対象**: α-3 ④（キーフレーム／場面内アニメ）。本 ADR は**設計のみ**。実装は α-4+。

---

## コンテキスト

ADR-0016 の **④ キーフレーム／場面内アニメ**を設計する。要素の移動・拡大・不透明度等を**時間で補間**し、場面＝連続フレームにする。`§10`「キーフレーム」を緩和し、**ADR-0001（場面内は静止・単一フレーム）を置換**する。④は ADR-0016 で**最重量**と位置づけられている。

### 現状と「既に用意された拡張点」
- ADR-0001（A2ハイブリッド）は **場面内を静止**とし、場面ごとに `layoutScene(scene, template)`→`layoutToSvg()`→PNG **1枚**を焼いて FFmpeg が尺ぶん保持する（`src/renderer/layout.ts`・`sceneSvg.ts`・`buildExportScenes.ts`）。動くのは動画スロットの中身と音声のみ。
- **重要**：ADR-0001 は**選択肢C＝「毎フレームをWeb描画してフレーム列→動画」を“Aの自然な拡張・将来オプション”として明記**（ADR-0001 の「選択肢C（将来オプション）」節・「決定」節）。**④はこの選択肢Cを発火させる**ものであり、描画方式の作り直しではなく**既存共有ロジックへの時間軸の導入**である。
- **パリティ**（ADR-0001 の生命線）：preview と export が**同一SVG＋同一ラスタライザ**（ADR-0004 WebView Canvas）を使えば一致は構造的に保証される（ADR-0001「スパイク結果」＝同一SVGを同一ラスタライザで描けば byte-identical）。④では**これをフレーム t ごとに拡張**する。
- **キーフレームの置き場**：ADR-0018（2モデル方式）で、時間軸の高度編集は**場面アンカーのオーバーレイ層**に保存し AI/簡易は無視、と決めた。④のキーフレームは**このオーバーレイに乗る**（ADR-0018 未解決6 を本ADRで回収）。

---

## 判断軸

1. **パリティ最優先の維持**（ADR-0001）：アニメでも preview＝完成品。**フレーム t の描画を preview/export で一致**させる。
2. **AI 親和性・単一パイプライン**（ADR-0007 M-A・ADR-0018）：キーフレームは**オーバーレイ**に置き、AI 出力・場面ベース正準は不変。AI はアニメを生成しない。
3. **後方互換・段階出荷**：**アニメの無い場面は従来どおり静止1枚**（経路・コスト不変）。アニメのある場面だけ毎フレーム化。
4. **非力PCでの速度**（`01 §15.2`）：尺×fps のコスト増を**選択的per-frame＋プレビュー軽量化**で抑える。
5. **§10 を必要分だけ緩和**：④は「要素の補間」。**口パク・3D・複雑エフェクトは引き続き §10 外**。

---

## 検討した選択肢

| 選択肢 | 評価 |
|---|---|
| **(A) 毎フレーム Web 描画＝ADR-0001 選択肢C の発火【採用】** | `layoutScene` に**時間 t** を与え、キーフレーム補間後のレイアウトを **SVG→PNG をフレームごと**に焼く。preview/export は同一経路・同一ラスタライザ＝**パリティをフレーム単位で構造保証**。既存共有ロジックの自然拡張。アニメ無し場面は静止1枚を維持。 |
| **(B) FFmpeg 側でアニメ（zoompan/rotate/fade フィルタ）** | 中間PNG不要で高速だが、**ブラウザの折返し・座標・体裁とズレ**＝ADR-0001 が選択肢B を退けた理由（パリティ破壊）と同じ。**不採用**。 |
| **(C) プレビューのみ CSS アニメ・書き出しは静止** | 実装は軽いが **preview≠export** で製品の生命線（パリティ）に反する。**不採用**。 |

---

## 決定（Proposed）

> **ADR-0001 の選択肢C（毎フレーム Web 描画）を発火させて場面内アニメを実装する。`layoutScene` に時間 t を導入し、キーフレーム補間後のレイアウトを SVG→PNG でフレームごとに焼く。キーフレームは ADR-0018 のタイムライン・オーバーレイに置き（AI/場面ベース正準は不変）、アニメの無い場面は従来どおり静止1枚を維持する。これにより ADR-0001 の「場面内静止」を（アニメ場面について）supersede する。**

### 描画モデル（per-frame）
- **`layoutScene(scene, template, timeSec?)`**：t 未指定＝従来の静止（後方互換）。t 指定＝**キーフレームを補間したレイアウト**を返す（domain 純粋関数・§7 テスト対象＝補間は決定論的）。
- **変換は「本来の状態」を基準とする相対値（CSS transform 相当）**：x/y＝本来位置からのオフセット／scale＝係数（中心維持）／rotation＝本来角度からのオフセット／opacity のみ絶対（0..1）。**要素を後から動かす/拡縮/回転しても再生・書き出しが自動追従**する（絶対焼き込みだと編集後に古い値へ固定される＝(2) レビュー対応）。適用は共有 `layoutScene` の1か所（preview/export 同一＝パリティ）。opacity は静的値との連動を `withEndOpacity`（終点だけ差替）で担保。
- **A2ハイブリッドは維持**：各フレームで「下PNG／動画スロット／上PNG」を ADR-0001 どおり分割。動画スロットは引き続き FFmpeg が合成。
- **選択的per-frame**：**キーフレームを持つ場面だけ** fps×尺ぶんのフレームを生成。持たない場面は**静止1枚**（経路・コスト不変＝後方互換）。
- **パリティ（フレーム単位）**：preview と export は**同一の `layoutScene(…, t)`＋同一SVG＋同一ラスタライザ**（ADR-0004）でフレーム t を描く＝ADR-0001 のパリティ保証を時間方向へ拡張。

### キーフレームのデータ（ADR-0018 オーバーレイに格納）
- キーフレームは **ADR-0018 の `timelineOverlay`**（場面アンカー・任意）に置く。**AI 出力・`project.scenes` 正準・簡易編集は不変**（AI はアニメを作らない＝`12` 変更なし）。
- 編集は**二層**（利用者合意 2026-07-02・ADR-0023 役割分担）：**簡易プリセット（「ふわっと表示」＝フェードイン等）は場面編集画面**（FREE 要素プロパティの隣で付け外し。FREE 要素は `§2-4` の座標例外＝そもそも手で配置する面）／**詳細なキーフレーム編集（複数KF・移動/拡縮/回転・イージング選択）は上位仕上げ＝タイムライン編集面**（別画面）に隔離する。データは同一の `timelineOverlay.animations`（どちらで触っても同じ・ADR-0023 の「両面で分断しない」）。当初 §54 は「編集はタイムライン別画面」としていたが、簡易プリセットに限り FREE の座標例外に沿って場面編集へ置くと明確化（`§2-4` の「通常編集で座標を毎回触らせない」は詳細キーフレーム編集に適用）。

### アニメ対象（初期サブセット・**決定**・利用者合意 2026-07-02）
- **プロパティ**：**位置（x/y）・拡大縮小（scale）・不透明度（opacity）・回転（rotation）** ＋ イージング（**linear / ease-in-out** で開始・他は将来）。
- **対象要素**：**FREE 要素＋グループ**（ADR-0022 のグループは元々アニメ対象として想定）。**通常テンプレのレイヤーは初期対象外**（テンプレ駆動 §2-4 との棲み分け＝座標はテンプレが決める。掛けたい要件が出たら別途拡張）。
- **見送り（§10 のまま）**：色トゥイーン・パス／モーション軌跡・マスク・**口パク**・3D。Ken Burns（背景のゆっくり寄り）は位置＋拡大の特例として表現可能。

### ADR-0001 の扱い
- 本ADR承認＋④実装時、ADR-0001 を **「Superseded by ADR-0019（アニメ場面の per-frame 描画について）」** に更新。**静止場面の A2 経路・パリティ原則・共有ロジックは存続**＝ADR-0001 の核は生きたまま、「場面内は常に静止」という制約だけを置換する。

---

## 結果・影響

- **コード（α-4）**：`layoutScene` に時間引数（または `layoutSceneAtTime`）＝補間の純粋関数（§7 テスト）／`renderer/export` がアニメ場面を**フレーム列**に展開（fps×尺）→ FFmpeg 連結（既存 concat/xfade を流用）／`renderer/preview` は同一補間で再生。**キーフレームのスキーマは ADR-0018 の `timelineOverlay` に内包**（④独自の新トップレベルは作らない）。
- **正典追補（実装着手時）**：`§10` の「キーフレーム」を**段階解除**（ADR-0012/0015/0018 と同型の注記）。`05_RENDERING_SPEC.md` に**per-frame 経路とフレーム単位パリティ**を追補。`11` にキーフレーム表現（オーバーレイ内）と補間契約を追記。`ADR-0001` の状態を Superseded（部分）へ。
- **パフォーマンス**：尺×fps（例 15s×30fps＝450フレーム/場面）。緩和＝**選択的per-frame**（アニメ場面のみ）／プレビューの**間引き fps・キャッシュ・スクラブ時オンデマンド**／長尺×多数アニメ場面の上限ガイド。`02` Phase 0 と同様に**小さく実証**してから本実装。
- **書き出しのフレーム配送＝ステージング**（実装知見）：アニメ場面のフレーム列（数百枚の PNG base64）を**1回の IPC（`invoke('export_video', {scenes})`）にまとめると `JSON.stringify` の文字列上限（V8 ≈512MB）を超えて `RangeError: Invalid string length` で失敗**する。よってフレームは **`stage_export_frame` で1枚ずつディスク（`<appData>/exports/.frames_stage/<dir>/frame_NNNNN.png`）へ逐次書き出し**、`export_video` には **`framesDir` 参照だけ**渡す（`framesBase64` を IPC に載せない）。メモリのフレーム蓄積も回避。書き出し前後で `clear_export_frames_stage`。
- **掛け合い×アニメ（③・実装知見）**：掛け合い場面もアニメ対応（`sceneAnimationActive` の掛け合い除外を撤廃）。書き出しは**行セグメントごと**に per-frame 化＝各行の区間 `[startSec, +durationSec]` を `layoutScene(t, animations, subtitleText)` で毎フレーム描画し、行ごとの音声を付けて連結（`sceneSegmentSpecs` に `startSec` を追加）。単一 narration は1区間として同経路。preview は行字幕（`activeLine`）＋アニメ（`timeSec`）を同一 `layoutScene` で描画（`ScenePreview`）。
- **動画スロット併用のアニメ（#435・ADR-0026 決定6・2026-07-06 追補）**：当初「動画スロット併用は静止」としていたが解除。**非掛け合いの動画スロット場面は、下/中/上の静止層すべてを per-frame（`layoutScene(t)`→`splitVideoSceneSvgMulti(t)` で各帯を切り出し）で焼き、Rust が image2 シーケンスとして合成する**（below=`tpad` で base 保持、mid/above=`overlay eof_action=repeat`）。これで背景/動画間/前景/グループの要素がプレビュー＝書き出しの両方で動く。**動画スロット本体（矩形）の位置/拡縮/回転/不透明度アニメも #442（2026-07-06）で対応**：`slotIsAnimated`（スロット層 or それを含むグループがアニメ対象か）が真のとき、buildExportScenes が場面を **(1) アニメ区間 [0,W]（W=`animationsEndSec`）は動画の実フレームをスロット矩形へ描いて場面全体を per-frame 合成（プレビュー＝ScenePreview と同一の `layoutScene(t)`＝完全パリティ・動画は「動きながら再生」）＋(2) settled 区間 [W,尺] は実動画を最終位置（`timeSec=animEnd` で収束）で流す**、の2セグメントに分割する。実装＝Rust `stage_clip_frames`（クリップの区間 [clipStart,+W*speed) を `setpts=PTS/speed,fps` で出力fpsサンプルし PNG 列にステージング）＋`read_export_frame`（ブラウザが実フレームを `assetSrc` 差し替えで層合成）＋窓の元音声は `build_window_audio` がナレーションと**元音声ON全スロット**を amix（`clipAudios[]`＝settled の Video 経路の全スロット amix とアニメ区間で同挙動・#442 P2）。**開始タイミングは既定=アニメと同時に先頭から再生**（`useRealFrames` 時は settled クリップを窓の続き `clipStart+W*speed` から連続再生）。窓=既存 Frames 経路／settled=既存 Video 経路で -c copy 連結。**「同時/途中/後」を選べる開始タイミング設定（schema＋UI）は後続PR＋ADR**。実フレーム合成できない場合（**非Tauri／コールバック未指定＝テスト等**）は代表フレーム＝サムネで代替（動画は settled から再生）。**抽出/読み出しの失敗は静かに代替せず書き出しを止める**（α-4方針＝壊れて見える代替より失敗停止・ADR-0026 判断軸④）。トリミング（`clipEndSec`）は窓の抽出/元音声を残り再生秒 `min(W,(clipEnd-clipStart)/speed)` に抑え、settled 開始も終点手前にクランプ＝切った後ろを読まない（通常動画経路の clipEnd 不変条件に合わせる）。**掛け合い×動画×アニメは引き続き未対応**（行区間×フレームの二重合成＝**場面形式では凍結ゆえ解除しない**・下記「決定済み」11＝**#512 は音量KFとタイムライン形式の動画素材へ再スコープ**・α-4 は #469 の UI ガード＝「設定できない理由を自然に示す」で close 済み・下記「決定済み」10）。`sceneAnimationActive` は非掛け合いで動画スロット有無に依らず true（掛け合い×動画のみ false）。
- **書き出し高速化＝アニメ終端で打ち切り＋最終フレーム保持（#376・実装知見）**：per-frame は「**変化する区間だけ**」焼けば十分。補間はアニメの**最終キーフレーム時刻 `animEnd`（`animationsEndSec`）以降レイアウトが一定**なので、`[区間開始, min(区間末, animEnd)]` だけをフレーム化し（+1で settled 到達）、残りは **FFmpeg 側 `tpad=stop_mode=clone:stop_duration={尺}` が最終フレームを尺いっぱいまで複製保持**する（末尾 `-t {尺}` でぴったり切る）。長い場面や掛け合い（アニメが頭だけの行）で per-frame ラスタライズを激減（例：45秒×1秒アニメ＝1,371枚→約31枚）。**パリティは不変**（`animEnd` 以降は同一フレームの繰り返し＝プレビューの静止と一致）。`scene_clip_args`（静止1枚）は従来どおり `-loop` で尺を満たす（映像フィルタ無し）。
- **AI（`12`）**：**変更なし**（AI はアニメを生成しない）。
- **段階出荷**（利用者合意 2026-07-02・入口＝(1)）：
  - **(1) per-frame 基盤＋1アニメで end-to-end 実証**：`layoutScene(scene, template, t?)` の補間（t なし＝静止で後方互換）＋書き出しが**アニメ場面だけフレーム列**に展開（静止場面は1枚維持）＋プレビュー＝export の同一補間。最小アニメ＝**FREE 要素の不透明度フェードイン**1種でパリティを実証。**(1c) オーサリング【実装済】**＝場面編集の「ふわっと表示」プリセットで FREE 要素（図形/文字/画像）にフェードインを付け外し（`fadeInKeyframes`・`addAnimation`/`updateAnimation`/`removeAnimation`）。**opacity を text/image にも適用**（従来は fill のみ＝`sceneSvg` が `<g opacity>` で要素全体を敷く）＝図形以外の写真・文字もふわっと表示できる。
  - **(2) 移動/拡縮/回転＋イージング**（linear/ease-in-out）。**【FREE 要素は実装済】**＝場面編集の簡易プリセットを **ふわっと（fade）/すべって（slide・上下左右）/ぽん（pop・拡縮）/くるっと（spin・回転）** に拡張＋**イージング選択（なめらか/一定）**。種類/所要秒/向き/イージングは keyframes から導出（`describeAnimation`）し**永続化しない**（schema 不変・1.17）。透明度スライダー連動は種類を変えず終点だけ更新（`withEndOpacity`）。**グループ対象は (3) へ**（layout のグループ合成と併せて実装）。
  - **(3) 複数キーフレーム・グループ・Ken Burns/プリセット**（フェード/スライド/ポップ/ゆっくりズーム/写真パン）。**【グループは実装済】**＝(2) の登場プリセットを**グループ全体**に付けられる（対象=`group_NNN`）。描画は**合成前に補間 transform を group.transform へ重ねる**（位置=加算/拡縮=乗算/回転=加算・中心不変＝`composeGroupGeometry` 再利用）＋opacity はメンバー要素へ乗算。プリセット builder は `el`→`endOpacity` に一般化し要素/グループ共用。**簡易プリセットは場面編集（グループ選択バー）**／**複数キーフレーム・Ken Burns/写真パンの手動編集はタイムライン形式**（#634 で land・当初の想定先だった ADR-0023 の統合編集面は ADR-0032 が置き換えた＝下記「決定済み」11）。
  - **(4) 音量キーフレーム**（**land 済み**＝タイムライン形式の `volumePoints`・timeline schema `"1.7"`・ADR-0032 の追補。下記「決定済み」11）。
  - 各版で `check:frontend` 緑＋**フレーム単位パリティ**確認。非力PC 向けにプレビュー間引き・キャッシュは (1) から織り込む。

---

## 決定済み（Accepted・2026-07-02）

1. **アニメ対象サブセット**＝位置(x/y)/拡縮(scale)/不透明度(opacity)/回転(rotation)＋イージング(linear/ease-in-out)。**FREE 要素＋グループ**（テンプレレイヤーは初期対象外）。（決定）
2. **プレビューのパリティ戦略**＝**二層**（正本＝再生/最終確認は export と同じ per-frame ラスタライズ／編集中スクラブは DOM 近似で軽く・**補間式は単一の `layoutScene(t)` を共有**して数値一致）。ADR-0023 の推奨に一致。（決定）
4. **fps**＝**30固定**（秒=正準・フレーム=量子化＝ADR-0023/0024 周辺方針）。プレビューは間引き・キャッシュ・スクラブ時オンデマンドで軽量化。（決定）
5. **要素の対象範囲**＝**FREE 要素＋グループ**（初期）。通常テンプレのレイヤーは §2-4 の棲み分けで初期対象外（要件次第で拡張）。（決定）
7. **口パク・3D・複雑エフェクト**＝§10 のまま**対象外**。（決定）
9. **アニメ編集UIの置き場**（利用者合意 2026-07-02・(1c)）＝**簡易プリセット（ふわっと表示）は場面編集画面／詳細なキーフレーム編集はタイムライン**（当初の想定先 ADR-0023 は **ADR-0032 のタイムライン形式**に置き換わった＝下記 11）。上記「キーフレームのデータ」の二層。データは同一の `timelineOverlay.animations`。（決定）
10. **α-4 出荷範囲と α-5 繰越の線引き**（決定・2026-07-13・利用者合意／Codex 監査 PR #511 P2＝繰越先の明確化）＝α-4 は **(1)〜(3) のうちグループ／簡易プリセット部**（＋per-frame land 済）を出荷。**残る高度アニメ＝(3残) 手動の複数キーフレーム・Ken Burns／(4) 音量キーフレーム／掛け合い×動画×アニメ（下記「結果・影響」§動画スロット併用の「後続」）は α-4 完了条件から外し、α-5 で完成させる**。**追跡先＝[#512](https://github.com/fujii-yuki-star/star-recruit-studio/issues/512)（④残の受け皿）**、編集の器は **ADR-0023／[#329](https://github.com/fujii-yuki-star/star-recruit-studio/issues/329)（統合タイムライン）**。α-4 の [#254](https://github.com/fujii-yuki-star/star-recruit-studio/issues/254) は「α-4 範囲の完了」として close し、残りは #512 が引き取る。[#469](https://github.com/fujii-yuki-star/star-recruit-studio/issues/469) は「設定できない理由を自然に示す」で α-4 close 済み。CLAUDE.md §10／§11 も同旨。（決定）**→ 繰越先と射程は下記 11 で改定した**（ADR-0023／#329 は ADR-0032 に置き換わり、#512 は「音量KF」と「タイムライン形式の動画素材」へ再スコープ＝**「#512 が完全対応」は取り消す**）。

11. **繰越先の変更（追補・2026-08-03・#512 の整理）**＝下記10 の「編集の器＝ADR-0023／#329（統合タイムライン）」は
    **ADR-0032（タイムライン編集＝別プロジェクト形式）で置き換わった**ので、繰越の行き先を実態に合わせて書き直す。
    - **(3残) 手動の複数キーフレーム・Ken Burns＝タイムライン形式で land**（#634）。任意の時刻に x/y/scale/rotation/opacity を
      置ける（値は「本来の見た目からのずれ」）。**Ken Burns は「位置＋拡大の2点」で書ける**＝専用のプリセットは持たない（**画面いっぱいに置いた写真の場合**。部分配置のクリップでは `scale` が箱ごと拡大し切り抜きも追従するので「枠を固定したまま中身だけ寄る」にはならない）。
      **場面形式は簡易プリセットのまま**（ADR-0032 の凍結＝場面編集の編集機能は広げない）。
    - **(4) 音量キーフレーム＝land 済み**（2026-08-03 設計 → 実装）。⚠️ **`Keyframe` には足していない**
      ＝音の合成は再生・書き出しの両方が区間単位で組む（`timelineAudioRuns`/`planBgmMix`）ので、
      **タイムライン形式のクリップが持つ `volumePoints`**（時間で音量を変える点列）として置いた
      （timeline schema `"1.7"`・`domain/timeline/volumePoints.ts`・正典は ADR-0032 の追補）。
    - **掛け合い×動画×アニメ＝場面形式は静止のまま**（`sceneAnimationActive` のゲートは凍結ゆえ解除しない）。
      **タイムライン形式は動画素材そのものが未対応**（置いてあると書き出しの手前で断る＝#631）ので、こちらでも
      「できる」状態ではない。**行区間×フレームの二重合成という当初の課題は、タイムライン形式では列を分ければ
      済む**（掛け合いに専用概念が要らない＝#633）ため、そのままの形では引き継がない。
    - **＝ 残る本当の未実装は「タイムライン形式の動画素材」の1つ**（**当初はここに「音量キーフレーム」も含めて2点へ再スコープした**が、音量の変化はその後 land した＝timeline schema `"1.7"`）。#512 は引き続きこの追跡先に畳む（**別起票しない**＝追跡先を増やさない）。（決定）

## 未解決の論点（実装着手 PR で確定）

3. **キーフレームのスキーマ**：ADR-0018 `timelineOverlay` 内での表現（要素参照・プロパティ・時刻・値・イージング）。実装PRで schema マイナーバンプ＋無変換移行として確定（ADR-0024 の非破壊編集モデルとも整合）。
6. **ADR-0001 supersede の粒度**：静止場面の A2 経路・パリティ原則を残しつつ「場面内静止」制約のみ置換する書き換え。**per-frame が land する PR で ADR-0001 を Superseded（部分）へ**。
8. **`layoutScene` のシグネチャ**：時間引数追加（`layoutScene(scene, template, timeSec?)`）か別関数か。**(1) の PR で最初に確定**（preview/export が同一補間関数を共有＝静止経路の後方互換も担保）。
