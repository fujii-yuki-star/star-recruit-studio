# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Env / secrets (never commit API keys)
.env
.env.*
!.env.example

# Claude Code local settings
.claude/settings.local.json

# Spike artifacts (ADR-0001 等の実験成果物)
.spike/

# 自動生成 ajv バリデータ（#156）。pre フックで毎回生成するため追跡しない（型は validators.d.ts を手書きで追跡）。
src/domain/validation/generated/validators.js
