# α-2 動作確認チェックリスト（開発・社内QA用）

α → α-2 で追加した機能を一通り確認するための項目・手順・期待結果。各項目に対応 issue を併記。
チェックは `- [ ]` を `- [x]` に。気づきは各項目の下にメモ。

---

## 0. 前提・準備

- [ ] **環境**：Windows 10/11。**packaged（MSI）での確認を推奨**（声＝VOICEVOX、書き出し＝FFmpeg を同梱で確実に動かせるため）。`npm run tauri dev` でも UI は確認可（声/書き出しはエンジンの有無に依存）。
- [ ] **声（VOICEVOX）**：起動直後は読み上げ準備で待つことあり。声が出ない時は少し待つ。
- [ ] **AI**：設定で接続キーを入れる or 「お試し」（Mock）。お試しでも一連の流れは確認できる（声は無音WAV）。
- [ ] **後方互換用**：**α 版で作った既存プロジェクトを1つ**用意（B-1 で使う）。

> 確認の基本動線：ホーム →「新しい動画を作る」→ 種類/目的/内容/素材 → 動画案生成 → 場面編集 → 仕上がり確認 → 動画を保存。

---

## A. 既存プロジェクトの後方互換（最優先・schema 1.x→1.8）

- [ ] **A-1** α で作った既存プロジェクトを開く → **壊れず開ける**。場面・素材・フォント・声・BGM が保持。保存し直し→再読込しても化けない。
  - 確認観点：schemaVersion が古くても読み込み時に現行へ移行（マイグレーション）。掛け合い未使用の場面は従来どおり。

---

## B. ゆうこ表示トグル削除（#171）

- [ ] **B-1** 場面編集を開く → **「ゆうこを表示」トグルが無い**（場面ごとの表示切替は廃止。ゆうこ表示はテンプレ依存）。

---

## C. FREE 自由配置編集の基本UX（#172 / #174 / #185 / #179）

> FREE 配置に入る：場面編集で **詳細編集（FREE テンプレ）** の自由配置エリア。

- [ ] **C-1 追加**：「＋テキスト / ＋素材 / ＋図形」で要素を追加できる。追加した要素が**自動選択**される。
- [ ] **C-2 移動・リサイズ**：ドラッグで移動、ハンドルでリサイズ。**Shift で縦横比を維持**（#172）。
- [ ] **C-3 グリッド**：「グリッドに合わせる」ON で吸着・グリッド表示。
- [ ] **C-4 複製・重なり順**：要素を**複製**、**前面/背面**で重なり順を変更（#172）。
- [ ] **C-5 ダブルクリック編集**：テキスト要素を**ダブルクリックで文字編集**（#174）。
- [ ] **C-6 右クリック「編集」**：要素を右クリック →「編集」で kind 別エディタが開く（#174/#185）。
  - テキスト：**文字の大きさ・色・太さ**を変更。
  - 図形：**種類・色・透明度**を変更。
  - 素材スロット：画像/動画を選び直し。
- [ ] **C-7 角の丸み廃止**：図形編集に**「角の丸み」項目が無い**（#185 で削除。角丸は専用図形 `rounded_rect` を使う）。
- [ ] **C-8 詳細編集モード（#179）**：「**選択した要素だけ編集**」ON → チップ（「テキスト1」等）で要素を切替え、選択中のカードだけ表示。未選択時は「上のボタンかプレビューで選んでください」。OFF で全カード表示（従来）。

---

## D. FREE 図形の拡張（#173・schema 1.6）

- [ ] **D-1 図形種類**：「＋図形」→種類で **四角 / 丸 / 三角 / 星 / 矢印 / 吹き出し / 角丸四角** を選べ、プレビューに正しい形で出る。
- [ ] **D-2 枠線**：図形に**枠線（色・太さ）**を付けられる。太さ 0 で枠線なし。
- [ ] **D-3 塗り/透明度**：塗り色・透明度を変えられる。

---

## E. 見た目パーツ（#175・方式A）

- [ ] **E-1** パーツ一覧から**主要パーツ（4〜5種）**を追加 → 複数の要素（図形＋テキスト等）が**まとまって配置**される。
- [ ] **E-2** 追加したパーツの**先頭要素が選択**される（所在が分かる）。各要素は個別に編集・移動できる。

---

## F. フォント（#178・schema 1.7、場面ごと=1.5）

- [ ] **F-1 動画全体**：設定で**動画全体のフォント**（3種：本文/見出し/演出）を変更 → 全場面に反映。
- [ ] **F-2 場面ごと**：場面編集で**この場面のフォント**を変更 →「**動画全体に合わせる**」で全体設定に戻る（継承）。
- [ ] **F-3 テキスト種別ごと**：タイトル/本文/字幕/キャプション/URL など**種別ごとにフォント上書き**。未設定は場面→全体→既定を継承。
- [ ] **F-4 FREE/パーツのテキスト**：FREE のテキスト要素ごとにフォント指定。未指定は場面/全体を継承。
- [ ] **F-5** プレビューと**書き出しMP4でフォントが一致**（ADR-0001 パリティ）。

---

## G. 声・ナレーション（#176 / #177 / #170）

- [ ] **G-1 話者選択（#177）**：設定で**声（キャラクター・スタイル）**を全VOICEVOX話者から選べる（グループ表示）。
- [ ] **G-2 初期自動生成（#176）**：動画案を作ると、各場面の**声が自動生成**される。場面編集で進捗「**声 X/Y**」が出る（生成中は「準備中…」）。
- [ ] **G-3 個別再生/作り直し**：場面編集で「▶再生」、「**声を作り直す**」で再生成。
- [ ] **G-4 失敗時**：声生成に失敗したら**エラー文言＋再生成導線**が出る（「もう一度お試しください」）。
- [ ] **G-5 クレジット動的化（#177/#170）**：選んだ話者に応じて**「VOICEVOX:＜キャラ名＞」のクレジット**がプレビュー/書き出し/About に表示（既定はずんだもん）。クレジットの**フォントは固定**（#170）。

---

## H. 掛け合い（EPIC #180・schema 1.8）★最重要

> 場面編集の「セリフ」欄に **「掛け合い（複数のセリフ）」トグル**。

- [ ] **H-1 ON/OFF**：トグル ON で単一セリフ →**セリフ列**に。複数行がある状態で OFF にすると**「2つ目以降が消えます」確認**（やめる/キャンセル）が出る。
- [ ] **H-2 行の追加/削除/並べ替え**：「＋セリフを追加」、各行「削除」、**↑↓で並べ替え**。最後の1行を消すと単一に戻る。
- [ ] **H-3 行ごと声（req7b）**：行ごとに**声（キャラクター）**を選べる（「動画全体の声に合わせる」＝継承）。
- [ ] **H-4 行ごと字幕（追加B）**：行ごとに**字幕の文言**＋**「字幕を表示する」ON/OFF**。文言空欄なら**セリフをそのまま字幕**に。
- [ ] **H-5 開始秒（簡易タイミング・PR-F3）**：行ごと「**開始（秒）**」入力。空欄＝順番に自動。場面尺を超える値は**範囲内にクランプ**。順序が前後/範囲外なら**注意文**が出る（「場面の長さ内にしてください」等）。
- [ ] **H-6 声をまとめて生成**：「**全部のセリフの声を作成**」→ 行ごとに音声生成。行ごとに「▶再生」。
- [ ] **H-7 プレビュー再生（追加A・PR-F2）**：仕上がり確認で掛け合い場面を再生 → **経過秒に合わせて字幕/フレームが切り替わり**、行ごとの声が順に鳴る。
- [ ] **H-8 書き出し**：掛け合い場面を含めて MP4 書き出し → **再生すると字幕が行ごとに切り替わる**（行＝セグメント）。
- [ ] **H-9 AI 掛け合い（PR-G・任意）**：AI で「掛け合い／2人で会話する」内容の動画案を作ると、場面が**複数セリフ（行ごと声）**で生成されることがある（best-effort・必須ではない）。未知のキャラ名は標準の声にフォールバック。

---

## I. 書き出し・BGM（横断）

- [ ] **I-1 MP4 書き出し**：「動画を保存」→ 保存先を選び MP4 出力 → **再生できる**。字幕・掛け合い・フォント・トランジション・BGM が反映。
- [ ] **I-2 BGM**：仕上がり確認で**標準BGM3曲＋自分の音楽**を選び ▶再生で雰囲気確認。書き出しにも反映。
- [ ] **I-3 縦型（9:16）**：縦型でも崩れず書き出せる（ADR-0012・α で対応済みだが回帰確認）。

---

## J. 横断チェック（規約・体験）

- [ ] **J-1 技術用語が出ない**：画面に JSON/templateId/speaker番号/lineId 等の**技術用語が出ない**（CLAUDE.md §2-3）。
- [ ] **J-2 エラーは次の行動**：エラー時の文言が**「どうすればよいか」**を示す（§2-5）。
- [ ] **J-3 操作の直感性**：詳細編集・掛け合いの操作が**迷わずできる**か（要望・引っかかりをメモ）。

---

### メモ・不具合
（画面 / 操作 / 結果 を記録。スクショがあると◎）
