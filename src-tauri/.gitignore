# Generated by Cargo
# will have compiled files and executables
/target/
# ⚠️ **検査用の別のビルド先**（#1118）＝`tauri dev` が `/target/` を掴んでいる間、
# `cargo clippy` / `cargo test` はファイルロックで走れない（os error 32）。
# `CARGO_TARGET_DIR` を分けて回すので、その置き場も無視する（**巻き込むと数GBがコミットに入る**）。
/target-check/

# Generated by Tauri
# will have schema files for capabilities auto-completion
/gen/schemas
