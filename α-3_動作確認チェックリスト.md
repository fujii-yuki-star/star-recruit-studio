# α-3 動作確認チェックリスト（開発・社内QA用）

α-2 → α-3 で**追加・改善した機能**を一通り確認するための項目・手順・期待結果。各項目に対応 issue を併記。
チェックは `- [ ]` を `- [x]` に。気づきは各項目の下にメモ。

> 📌 α-2 までの機能（掛け合い基盤・ボイス選択・見た目パーツ・FREE 直接編集 等）は `α-2_動作確認チェックリスト.md` を参照。本書は **α-3 の差分**に絞っています。

---

## 0. 前提・準備

- [ ] **環境**：Windows 10/11。**packaged（MSI）での確認を推奨**（声＝VOICEVOX、書き出し＝FFmpeg を同梱で確実に動かせるため）。`npm run tauri dev` でも UI は確認可。
- [ ] **声（VOICEVOX）**：起動直後は読み上げ準備で待つことあり。声が出ない時は少し待つ。
- [ ] **AI**：設定で接続キー or 「お試し」（Mock）。お試しでも一連の流れは確認できる。
- [ ] **後方互換用**：**α-2 で作った既存プロジェクトを1つ**用意（A-1 で使う）。

---

## A. 後方互換（最優先・schema 1.8 → 1.13）

- [ ] **A-1** α-2 で作った既存プロジェクトを開く → **壊れず開ける**。場面・素材・フォント・声・BGM・掛け合いが保持。保存し直し→再読込しても化けない。
  - 観点：1.8→1.9（回転）/1.10（行間・揃え・縁取り）/1.11（非表示・ロック）/1.12（行ごとの抑揚）/1.13（場面ごとの収め方）の追加は**任意フィールド**＝読み込み時に現行へ移行され、未設定は既定を継承。

---

## B. 詳細編集の磨き込み（FREE 自由配置・#205–#211）★今回の主役

> FREE 配置に入る：場面編集で **こだわり編集（詳細編集）** の自由配置エリア。

- [ ] **B-1 整列・スナップ強化（#205）**：複数要素を**整列**（左/右/中央/上/下）・**分布**（均等配置）。ドラッグ時に**整列ガイドが出て他要素にピタッと吸着**。
- [ ] **B-2 複数選択・一括操作（#206）**：**Shift＋クリックで複数選択** → まとめて移動・削除・整列。
- [ ] **B-3 コピー＆ペースト（場面内・場面間／#207）**：要素をコピー → **同じ場面**にも**別の場面**にも貼れる（場面を移動しても貼り付け可）。
- [ ] **B-4 数値入力（#208）**：選択要素の **横位置 / 縦位置 / 幅 / 高さ / 角度** を**数値で**正確に入力できる。
- [ ] **B-5 テキスト体裁の拡充（#209）**：FREE テキストに **行間 / 揃え（左右中央）/ 縁取り（色・太さ）** を設定でき、プレビューに反映。
- [ ] **B-6 レイヤー一覧パネル（#210）**：重なり順を**一覧で操作**（↑↓）。各レイヤーの**非表示**・**ロック**を切替。ロック中は移動/拡縮できない。
- [ ] **B-7 取り消し・やり直し（Undo/Redo・#211）**：編集（移動・削除・整列・貼り付け等）を **Undo で戻し、Redo でやり直せる**。連続ドラッグは1回で戻る。

---

## C. プロジェクト管理（ホーム画面・#212 / #241）

- [ ] **C-1 削除（#212）**：ホームの「最近のプロジェクト」で🗑 → **確認（取り消し不可の明示）** → 「削除する」で**一覧から消える**。「やめる」で残る。
- [ ] **C-2 名前変更（#241）**：✏ → **その場で名前を編集** →「保存」で反映（更新日も最新化）。Enter で保存・Esc でやめる。**空名は保存できない**。日本語入力の変換確定 Enter で誤保存しない。

---

## D. テンプレ作成・編集エディタ（見た目パターン・#214 / #250 / #271）★新機能

> 「見た目パターン」画面（一覧）から「複製して編集／編集」で**専用の編集画面へ遷移**して編集する（#271）。標準（同梱）は読み取り専用＝複製してから編集。

- [ ] **D-0 専用編集画面（#271）**：一覧で見た目を選び「複製して編集」（自作なら「編集」）→ **編集画面へ切り替わる**。広いプレビュー＋右側に名前・レイヤー・型別設定。「← 一覧へ戻る」で戻る（未保存があれば確認）。「変更を保存」で一覧へ戻り反映。一覧画面に小さな編集欄が出ていた旧UIではなくなっている。

- [ ] **D-1 ゼロから作成／複製編集（#214）**：**ゼロから新規作成**、または既存を**複製して編集**できる（向き・カテゴリ・名前を設定）。
- [ ] **D-2 レイヤー編集（#214）**：レイヤーを**追加・削除・配置**（ドラッグ/リサイズ/整列・吸着/重ね順）。種類ごとに設定：文字＝表示テキスト/大きさ/色/太さ、図形＝形/色、素材＝入れるもの/収め方、背景＝背景色 ほか。
- [ ] **D-3 場面のテキスト欄が連動（#214 ④b）**：作った見た目パターンを場面に使うと、場面編集の**テキスト欄がそのテンプレのテキスト層に応じて出る**（見出し/本文/字幕 等）。
- [ ] **D-4 立ち絵のポーズ（#250）**：character（立ち絵）レイヤーで **「ポーズ（既定）」** を選べる（候補はゆうこ素材の表情）。素材が無いと「素材から増えます」の案内。
- [ ] **D-5 グローバル保存（#214）**：作った見た目パターンは**別のプロジェクトからも選べる**（共通保存）。削除すると、それを使っていた場面は標準へ自動置換。
- [ ] **D-6 AI には出さない（#214）**：AI の自動おまかせには自作テンプレは出ない（手動選択でのみ選べる）。

---

## E. 掛け合い・声の強化（#242 / #243 / #249）

- [ ] **E-1 行ごとの声調整（#242）**：掛け合いの各セリフ行で「**声の調整（速さ・高さ・抑揚）**」を開き、行ごとに調整。「全体に合わせる」で継承に戻る。変更すると声は作り直し。
- [ ] **E-2 単一ナレーション場面の声調整（#249）**：掛け合いでない**普通の場面でも**、ナレーションの「声の調整（速さ・高さ・抑揚）」ができる（行ごとと対称）。
- [ ] **E-3 クレジットの話者連動（#243）**：掛け合いで**行ごとに声（キャラ）を変える** → 書き出し動画で、**しゃべっているキャラに合わせてクレジット表示が切り替わる**。プレビューでも有効行の話者に連動。動画＋掛け合いの場面は使用キャラを併記。

---

## F. 横断チェック（規約・体験・パリティ）

- [ ] **F-1 技術用語が出ない**：画面に JSON/templateId/speaker番号/poseTag 等の**技術用語が出ない**（§2-3）。
- [ ] **F-2 エラーは次の行動**：失敗時の文言が**「どうすればよいか」**を示す（§2-5）。
- [ ] **F-3 プレビュー＝書き出し（パリティ）**：詳細編集・テキスト体裁・クレジットが、**プレビューと書き出しMP4で一致**（ADR-0001）。
- [ ] **F-4 操作の直感性**：詳細編集・テンプレ作成・声調整が**迷わずできる**か（要望・引っかかりをメモ）。

> ⚠️ 既知：動画スロット＋掛け合いの場面は、プレビューのクレジットが書き出しと一部一致しない（[#248](https://github.com/fujii-yuki-star/star-recruit-studio/issues/248)・書き出し自体は正しい）。

---

### メモ・不具合
（画面 / 操作 / 結果 を記録。スクショがあると◎）
